# 日報承認・差戻し 実装後レビュー

## 最終判定

- 対象: 日報承認・差戻しのBackend、Frontend、Mock E2E、DDL入口、CI/品質ゲート。
- 判定: Task 6最終レビューのImportant 4件、Minor 2件を修正済み。最終再確認でCritical/Important/Minor 0件を確認する状態とした。
- DDL: ユーザー側で適用済み。適用内容の正本は `backend/src/main/resources/db/oracle/schema-daily-report-approval.sql`。

## 受入条件・レビュー指摘の対応

| 区分 | 結果 |
| --- | --- |
| 承認 | 上長の確認ダイアログで確定後に1回だけAPIを呼び、`APPROVED`、承認者、承認日時を反映 |
| 差戻し | trim済みコメントを保存し、`REJECTED`、差戻し者、差戻し日時、コメントを反映。社員の修正・再提出へ接続 |
| 権限・状態 | 担当上長のみ操作可能。社員/管理者、担当外、PENDING以外、不存在を拒否 |
| 一覧・詳細 | 担当グループのPENDINGだけを一覧表示し、承認後は一覧から除外。詳細で監査情報を表示 |
| 操作安全性 | 承認確認、キャンセル/Escape、フォーカス復帰、背景inert、二重送信抑止を追加 |
| テスト構成 | API/UI/E2Eを責務別に分割し、TC/RTを実テストへ追跡 |

## 実行済み検証

| 品質ゲート/コマンド | 結果 |
| --- | --- |
| `scripts/check.ps1 -Mode Full` | 成功。Frontend lint/typecheck/unit/build、Backend compile/Spotless/Checkstyle/SpotBugs、契約テストを通過 |
| `scripts/check.ps1 -CiTask E2E` | 成功。E2E TypeScript型チェック、production build、Playwright 16/16 |
| `scripts/check.ps1 -CiTask FrontendCoverage` | 成功。14 files、125 tests、Statements 96.36%、Branches 94.58%、Functions 97.23%、Lines 96.22% |
| Approval Mock E2E | 成功。3/3 |
| 日報既存E2E | 成功。13/13 |
| Oracle focused API | 成功。Approval 15、Search 17、Submission 5、計37件、Failures/Errors/Skipped 0 |
| `scripts/check.ps1 -Mode Quick` | `gitleaks`未導入のためstaged-secretsのみ失敗。whitespace/artifactは成功 |
| `scripts/check.ps1 -CiTask BackendCoverage` | テスト107件は成功したが、`jacoco.exec`とレポート3ファイルが生成されず失敗。数値は正式判定しない |

## Task 6最終レビュー指摘

- `FIND-APR-FINAL-001`: **既存観点で対応**。承認確認ダイアログ、キャンセル/Escape、API未呼出、確定1回、二重送信抑止をUI/E2Eへ追加。
- `FIND-APR-FINAL-002`: **標準化候補**。`typecheck:e2e`、`e2e:run`、`check.ps1 -CiTask E2E`、GitHub Actions、品質ゲート運用を接続。
- `FIND-APR-FINAL-003`: **既存観点で対応**。APPROVED/REJECTEDの非null監査列をfixtureへ設定し、invalid-state全列不変とapprove/reject 404を追加。
- `FIND-APR-FINAL-004`: **既存観点で対応**。セキュリティ・可読性・実装後レビュー文書、受入条件、テストケース、作業記録、指摘一覧を整備。
- `FIND-APR-FINAL-005`: **既存観点で対応**。承認Serviceの重複認可を`DailyReportAccessPolicy`へ統合。
- `FIND-APR-FINAL-006`: **既存観点で対応**。Backend/UI/E2EのTC/RT対応を直接確認可能にした。

## 未完了・再確認条件

- fresh schema resetと実Backend接続E2Eは未実行。専用TEST schemaを安全にresetできるrunnerが利用可能になった時点で実行する。
- Backend JaCoCoはagent適用経路またはSurefire設定を確認し、実行データとレポート3ファイルを生成できた時点で再実行する。
- `gitleaks`はローカル環境へ導入後、QuickおよびCIの秘密情報ゲートを再実行する。今回の差分に資格情報・保護設定を含めていないことは確認済み。
