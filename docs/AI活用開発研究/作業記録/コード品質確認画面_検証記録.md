# コード品質確認画面 検証記録

## 対象

- 機能: `F-001` ログイン
- 成果物: `docs/AI活用開発研究/コード品質確認画面/F-001_ログイン/`
- 実装モード: 簡易修正モード

## 確認済み

| 確認項目 | 実行内容 | 結果 |
| --- | --- | --- |
| JSON構文 | `ConvertFrom-Json`で表示用JSONを読み込む | 成功 |
| 表示用データ件数 | 受入条件6件、ケース35件、観点15件、実テスト35件を確認 | 成功 |
| JSON参照整合性 | 受入条件、ケース、実テスト、証跡、指摘、観点のID参照を生成スクリプトで検証 | 成功 |
| PowerShell構文 | `Parser.ParseFile`で生成スクリプトを解析 | 成功 |
| ValidateOnly | `F-001_ログイン_生成.ps1 -ValidateOnly` | 成功 |
| HTML生成 | 生成スクリプトで自己完結HTMLを生成 | 成功、約63KB |
| 埋め込みJSON | HTML内の`quality-data`を`ConvertFrom-Json`で再確認 | 成功 |
| 外部依存なし | `fetch(`、`http://`、`https://`、`jdbc:`、未解決テンプレートを検索 | 検出なし |
| HTMLスモーク | JSDOMで概要カード6個、受入条件行6個、ケース詳細35個、タブ切替を確認 | 成功 |
| Docs Simpleゲート | `scripts/check.ps1 -Mode Simple -Scope Docs -FocusedUnitNotApplicableReason ...` | 成功（実行ロジックなし、Markdown lintのみ） |

## 簡易モードで未実行

| 確認項目 | 未実行理由 | 再確認条件 |
| --- | --- | --- |
| アプリ本体Full | 今回は開発資料の静的画面のみを変更 | アプリ本体の実装を変更した場合 |
| Frontend E2E | 品質確認画面の生成物確認であり、アプリ操作を変更していない | HTMLをアプリへ組み込む場合 |
| Oracle実機テスト | アプリ本体を変更していない。既存ログインレビューでは`ORA-01017`で保留 | Oracle資格情報復旧後にF-001を再確認する場合 |
| Backendカバレッジ再生成 | 既存JaCoCo成果物を読み取り、今回の表示画面作成では再生成しない | Backendテストを実行できる環境が整った場合 |

## 表示上の保留・不足

- Backend JaCoCoは既存レポートを読み取り、全体Branch 79.71%が基準未達のため「未通過」として表示している。
- `AuthController`のBranch 1/2を未通過分岐として表示している。
- Backend・Oracleケースは既存レビュー記録に合わせて、資格情報エラーによる未実行として表示している。
- `BE-AUTH-010`、`BE-AUTH-012`、`FE-AUTH-011`は、既存レビューの期待結果補足を「不足」として表示している。
- Oracle接続環境の再確認は、既存のログイン機能レビュー記録に従う。

## 証跡

- 生成HTML: `../コード品質確認画面/F-001_ログイン/F-001_ログイン_コード品質確認.html`
- 表示用JSON: `../コード品質確認画面/F-001_ログイン/F-001_ログイン_表示データ.json`
- 生成スクリプト: `../コード品質確認画面/F-001_ログイン/F-001_ログイン_生成.ps1`
