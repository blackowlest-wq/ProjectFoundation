# Skill配置移行 作業記録

## 依頼内容

ProjectFoundation用Skillのリポジトリ内配置を、Codexの現行リポジトリSkill配置へ合わせる。

## 実装対象

- 編集元Skillを `docs/AI活用開発研究/構想メモ/標準化/skills/` に置く。
- Codexが自動検出する正式配置へ、次のSkillをコピーする。
  - `.agents/skills/projectfoundation-preflight-ja/SKILL.md`
  - `.agents/skills/projectfoundation-review-ja/SKILL.md`
- `AGENTS.md` と `標準化資料一覧.md` に編集元、正式配置、更新手順を記載する。

## 対象外

- ユーザー領域 `C:\Users\user\.codex\skills` のSkillは変更しない。
- Skillの内容、トリガー条件、レビュー手順の仕様変更は行わない。
- 日報編集画面の機能実装はこの移行とは分けて行う。

## 確認した外部ドキュメント

- Codex Manual「Build skills」: リポジトリSkillの正式配置は `.agents/skills` と確認した。

## 変更内容

- `AGENTS.md` にProject Skillの配置・編集元・同期ルールを追加した。
- `docs/AI活用開発研究/構想メモ/標準化/標準化資料一覧.md` にSkillの編集元・正式配置・更新手順を追加した。
- 既存のProjectFoundation用Skillを正式配置へコピーした。

## 判断・仮定

- 既存Skillの内容は現行プロジェクト標準に沿っているため、今回は配置移行のみとした。
- 旧配置のSkillは編集元として残し、正式配置先との同期対象とした。

## 実行した確認

| 確認内容 | 結果 |
| --- | --- |
| 編集元Skillの存在確認 | 実施 |
| 正式配置Skillの存在確認 | 実施 |
| 編集元と正式配置先の内容一致 | SHA-256一致を確認 |
| Skill frontmatter検証 | `python -X utf8 C:\Users\user\.codex\skills\.system\skill-creator\scripts\quick_validate.py` で編集元・正式配置先の4ディレクトリがすべて `Skill is valid!` |
| Markdownlint | `npm run lint:markdown -- --no-globs` で対象5ファイルがエラー0 |
| Git差分確認 | `git diff --check` を実施 |

## 発生した問題・手戻り

- 初回Markdownlintで作業記録末尾の連続空行を1件検出し、修正後に再実行してエラー0とした。
- 初回Skill検証で既存ファイルのUTF-8 BOMがfrontmatter判定を妨げたため、内容を変えずにBOMなしUTF-8へ正規化して正式配置へ再コピーした。

## 未実行・保留事項

- Skillの実タスクによる前方互換性確認は、日報編集画面の実装前確認で実施する。
