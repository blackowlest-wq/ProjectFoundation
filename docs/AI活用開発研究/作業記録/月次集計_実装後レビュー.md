# 月次集計 実装後レビュー

## 1. 対象とレビュー範囲

- 対象機能: `F-011 月次集計`、`A-016 GET /api/monthly-summaries`、`S-008 月次集計画面`
- モード: 品質重視モード
- 対象範囲: Backendの認可・年月検証・4 SQL集計・DTO、FrontendのPage/App/API、Mock E2E、Oracle E2E runner/fixture、観測可能性、記録
- 対象外: F-012 CSV、集計バッチ、ページング、マスタ保守
- 実装前レビュー: Backend/DB/Security、Frontend/UI/配置、期待結果/Oracleの3独立レビューを実施し、正本・実装計画へ反映済み
- 実装後レビュー担当: Backend/DB/Security=`postreview_backend`、Frontend/UI=`postreview_frontend`、期待結果/Oracle=`preflight_expectations`、統合=`/root`

## 2. 仕様・責務・セキュリティ確認

| 観点 | 判定 | 根拠 |
| --- | --- | --- |
| 仕様整合 | 実装コードは適合 | ADMIN限定、厳密`yyyy-MM`、半開区間、`APPROVED`限定、4固定配列、null勤務分0を実装。 |
| 責務分離 | 適合 | `MonthlySummaryController`→`MonthlySummaryService`→`MonthlySummaryRepository`、Page→API clientの責務を分離。 |
| 入力・認可 | 実装/Unit確認済み | EMPLOYEE/MANAGERは年月解析・Repository呼出しより前に403。形式不正は400共通エラー。 |
| ログ・相関ID | 部分確認 | 月次path metadataと401完了ログ、Oracle specのoperation ID captureは追加済み。200/403の実ログと機密値非出力はOracle/CI未実行。 |
| DB整合性・性能 | コード/SQL形状確認済み | 4 bind SQL、既存索引利用、N+1なし、`readOnly=true`。状態別・境界・read-onlyのOracle結果は未取得。 |
| UIアクセシビリティ | 実装/Unit/Mock確認済み | `tablist/tab/tabpanel`、`aria-controls/labelledby`、自動選択、左右矢印、非選択panel hiddenを確認。 |
| 配置・共通化 | 適合 | 月次feature/package、月次fixture/supportを分離。汎用UI helperは`uiTestSupport`へ集約し既存日報supportから再利用。 |

## 3. 指摘と判定

### 対応済み

- `FIND-F011-POST-FE-001`（既存観点で対応）: Unit/Mock E2E/Oracle specの正常fixtureを社員・案件・分類・休日の行単位完全一致へ強化し、hidden panelも確認。
- `FIND-F011-POST-FE-002`（既存観点で対応）: TC-F011-FE-005の空状態を共通文言へ具体化し、実装とテストを一致。
- `FIND-F011-POST-TRACE-001`（既存観点で対応）: 本レビュー、統合品質記録、作業記録、指摘一覧を実装後状態へ同期。
- SpotBugsの`EI_EXPOSE_REP/EI_EXPOSE_REP2`（個別対応）: 月次Responseの配列を防御的にコピーし、Spring管理`JdbcTemplate`注入の既存除外規約へ対象クラスを追加。Full Backend品質ゲートで0件を確認。

### 未達・保留

- `FIND-F011-POST-BE-001`（P0、標準化候補）: BE-002～006、BE-010、BE-012、BE-013の実DB fixture/ログ証跡が不足。代表Controller testは共通Oracle runnerで7/7成功したが、状態別・境界・read-only・実ログの追加証跡は未達。
- `FIND-F011-POST-GATE-001`（P0、既存観点で対応）: 直接Maven実行時の`ORA-01017`は共通Oracle runner迂回が原因で、wrapper再実行によりController DB 7/7は成功。承認→月次画面→ログ→verifyを含むE2EOracleは安全制約により未実行のため、`F-011-AC-012`と機能完了判定は保留。
- `FIND-F011-POST-CONN-001`（P0、既存観点で対応）: 月次Controller fixtureの`daily_reports` INSERTが24列に対して23プレースホルダで、接続後にOracle 17003「列索引が無効です」となった。24個へ修正し、共通Oracle runnerで7/7成功。fixture列数と引数数の一致をDBテスト観点へ記録。
- `FIND-F011-POST-SEC-001`（P1、既存観点で対応）: 200/403完了ログ、403のrequestId header/body一致、query/year/name/SQL非出力の実ログ捕捉が未達。
- `FIND-F011-POST-DB-001`（P2、標準化候補・保留）: `9999-12`の翌月初とOracle DATE上限は実機確認待ち。仕様にない年範囲制限は追加しない。

## 4. テスト・品質ゲート

### 成功

- Frontend focused: `npm.cmd --prefix frontend run test -- --run test/MonthlySummaryPage.test.tsx test/monthlySummaryApi.test.ts test/App.test.tsx`（3 files / 23 tests）
- Frontend全体: `npm.cmd --prefix frontend run test`（18 files / 150 tests）
- Frontend static/build: `npm.cmd --prefix frontend run lint`、`typecheck`、`typecheck:e2e`、`build`、`build:ci`
- Mock E2E: `npm.cmd --prefix frontend run e2e -- monthly-summary.spec.ts`（4/4）、`approval-rejection.spec.ts`（4/4）
- Backend focused: `MonthlySummaryServiceTest`、`JdbcMonthlySummaryRepositoryTest`、`RequestMetadataInterceptorTest`、`RequestIdFilterTest`（成功）
- Backend static: `test-compile`、Spotless、Checkstyle、SpotBugs（0 bugs）、PMD（成功）
- FullゲートのFrontend（lint/typecheck/unit/build）と契約テスト（Oracle preflight、coverage、impact、PMD）は成功。

### 未実行・成功扱いにしない項目

- `MonthlySummaryControllerTest`: 直接Mavenでは環境変数未設定で`ORA-01017`。共通Oracle runnerで再実行し、7/7成功。初回失敗は実装欠陥ではなく実行入口の差分、次の失敗はfixtureプレースホルダ不一致だった。
- Oracle/E2EOracle: 固定データのcleanup/verifyを伴うため、ローカル安全制約により実行していない。read-only identity checkは成功したが代替証跡ではない。
- Coverage: 本機能専用のFrontendCoverage/BackendCoverageは未実行。

## 5. トレーサビリティ

| RT | 実テスト | 実行結果 |
| --- | --- | --- |
| RT-F011-BE-001/006/007/012 | `MonthlySummaryServiceTest`の各`RT_F011_BE_*`メソッド | focused成功 |
| RT-F011-BE-SQL | `JdbcMonthlySummaryRepositoryTest` | SQL形状focused成功 |
| RT-F011-BE-011/013 | `RequestMetadataInterceptorTest`、`RequestIdFilterTest` | focused成功。実DBログは保留 |
| RT-F011-FE-001 | `frontend/test/monthlySummaryApi.test.ts` | focused成功 |
| RT-F011-FE-002/004/005/006/007/009/010/011 | `frontend/test/MonthlySummaryPage.test.tsx`の同名`it` | 8 tests成功 |
| RT-F011-FE-008/012/013 | `frontend/test/App.test.tsx`の`RT-F011-FE-008`、`RT-F011-FE-012-013`月次接続ケース | App 14 tests成功（全23 focused / 全150成功） |
| RT-F011-E2E-001/002/003 | `frontend/e2e/monthly-summary.spec.ts` | 4/4成功 |
| RT-F011-ORACLE-001 | `frontend/e2e/monthly-summary.oracle.spec.ts`、runner/verify/cleanup | 未実行。Oracle CIで再確認 |

## 6. 完了判定

通常のFrontend/Backend静的・Unit・Mockゲートは成功した。DB集計の完全性と実Backendの承認→月次集計はOracle証跡がないため、`F-011-AC-012`およびF-011の最終完了判定は保留する。

再確認条件は、対象commitを資格情報付きOracle CIで`E2EOracle`実行し、BE-002～006/010/012/013のDB・ログ証跡、F-011専用Playwright report、Backend log、verify結果、cleanup結果を回収することである。
