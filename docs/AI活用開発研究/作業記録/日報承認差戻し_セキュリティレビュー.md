# 日報承認・差戻し セキュリティレビュー

## 対象と判定

- 対象: 日報承認・差戻しのBackend API/Service/認可、Frontend操作UI、Mock E2E、DDL入口、監査ログ、品質ゲート。
- 根拠: 承認済み設計書、`セキュリティ規約.md`、`実装後レビュー表.md`、Task 6最終レビュー指摘 `FIND-APR-FINAL-001`～`006`。
- 判定: Critical 0件、Important 0件、未解決Minor 0件。Task 6で検出された6件はすべて対応済み。

## 観点別確認

| 観点 | 確認内容 | 結果・証跡 |
| --- | --- | --- |
| 認証・CSRF | 未認証401、CSRF未指定/不正403、共通エラーを承認・差戻し双方で確認 | Oracle focused 37件中に含め、Failures/Errors/Skipped 0 |
| ロール・担当範囲 | 上長のみ操作可能、担当グループ外を拒否、社員/管理者の操作UI/APIを抑止 | `DailyReportAccessPolicy`を承認Serviceでも再利用。Controllerテスト、UIテスト、E2Eで確認 |
| IDOR・情報非露出 | 担当外上長は403、監査ID/氏名/日時/コメントを返さない。不存在は404 | `DailyReportApprovalControllerTest`のRT-APR-BE-008で確認 |
| 状態遷移 | `PENDING`以外は409、承認/差戻し成功時だけ監査列を更新 | APPROVED/REJECTEDのDB行を更新前後で比較し、不変項目を確認 |
| 入力検証 | 差戻しコメントのtrim、空白のみ/上限超過、DB不変 | Oracle focused testで境界と失敗時副作用を確認 |
| UI安全性 | 承認確認、二重送信抑止、キャンセル/Escape、背景操作停止、差戻しダイアログ | `DailyReportDetail.tsx`とUIテスト、approval Mock E2Eで確認 |
| ログ・機微情報 | 業務イベントのfeature/useCase/requestIdを保持し、本文/氏名/tokenを追加しない | Oracleログと既存観測性テストで確認 |
| 秘密情報 | 変更差分に資格情報・保護Oracle設定を含めない | `gitleaks`未導入のため自動検査は未実行。差分を目視確認し、保護設定は追跡対象外のまま維持 |

## Task 6指摘の標準化判定

- `FIND-APR-FINAL-001`: **既存観点で対応**。確認操作、二重送信、モーダル安全性は既存のUI安全側・アクセシビリティ観点で検出可能。
- `FIND-APR-FINAL-002`: **標準化候補**。E2E TypeScript型チェックを`package.json`、`scripts/check.ps1 -CiTask E2E`、CI、品質ゲート運用へ接続した。
- `FIND-APR-FINAL-003`: **既存観点で対応**。状態遷移後の全監査列不変と不存在境界は既存のDB整合性・状態・API観点で検出可能。
- `FIND-APR-FINAL-004`: **既存観点で対応**。実装後の専門レビュー、指摘一覧、受入条件、テストケース、作業記録の同期は既存のレビュー運用で検出可能。
- `FIND-APR-FINAL-005`: **既存観点で対応**。共通認可部品の再利用は共通部品化・認可レビュー観点で検出可能。
- `FIND-APR-FINAL-006`: **既存観点で対応**。TC/RTの実テスト名への追跡はトレーサビリティ観点で検出可能。

## 制限事項

- fresh schemaのリセットと、実Backend接続のPlaywright E2Eは、環境を破壊しないため未実行。専用TEST schemaと安全なreset runnerが利用可能になった時点で再確認する。
- BackendCoverageはOracleテスト107件が成功したが、`target/jacoco.exec`が生成されず、JaCoCoレポート3ファイルの存在確認で失敗した。JaCoCo agent/Surefire適用経路を修正後、数値を正式判定する。
