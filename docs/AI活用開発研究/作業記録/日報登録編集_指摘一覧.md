# 日報登録・編集画面 指摘一覧

## 目的

ここしばらくの日報登録・編集画面に対するレビュー、共通化検討、DBマスタ化検討で発生した指摘を、作業記録とは別に追跡できるよう一覧化する。

作業記録は「何をしたか」を残す資料、指摘一覧は「何を問題として検出し、どう扱ったか」を残す資料として分ける。

## 指摘一覧

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| FIND-CQ-001 | 品質確認画面・自動判定 | Medium | 品質確認画面の「不足」「要補足」にAIまたは意味評価が混在すると、モデル変更やハルシネーションで同じデータの表示結果が変わる可能性がある | 対応済み | `F-001_ログイン_生成.ps1`で必須項目、ID、紐付け、実行結果、品質ゲート、カバレッジ、明示的な保留だけを`automatedFindings`として生成し、レビュー状態は入力記録へ分離。`コード品質確認画面_検証記録.md`、`実装後レビュー表.md`、`テストケースレビュー観点.md`へ反映 |
| DR-R-001 | 可読性 | Medium | `DailyReportForm` にマスタ読込、URL解析、状態管理、API呼び出し、JSXが集中していた | 対応済み | `DailyReportForm.tsx`、`useDailyReportEditor`、`WorkItemsEditor` に分割。`日報登録編集_可読性レビュー.md` |
| DR-R-002 | 可読性 | Medium | `TimeRules.calculate` が入力検証、勤務時間計算、休日区分判定を同時に担っていた | 対応済み | `validateAndCalculate`、休日区分別検証、勤務時間計算へ分割。`日報登録編集_可読性レビュー.md` |
| DR-R-003 | 可読性 | Medium | 休憩・深夜時間帯が分単位の直値や `int[]` で表現され、意味を読み取りにくかった | 対応済み | `TimePeriod` に置き換え。後続のDBマスタ化で `break_type_periods` / `work_time_types` 参照へ変更 |
| DR-R-004 | 可読性 | Low | 登録・編集時に検証と適用で同じ勤務時間計算を二度呼んでいた | 対応済み | 計算結果を `DailyReportService` で一度だけ作成し、`applyContent` に渡す形へ変更 |
| DR-R-005 | 共通化 | Low | `authApi.ts` と `dailyReportApi.ts` にCookie、CSRF、JSON、エラー処理が重複していた | 対応済み | `shared/apiClient.ts` に共通化。`共通化チェック観点_2026-06-30.md` |
| DR-R-006 | ファイル配置 | Low | 日報ControllerにマスタAPIが同居していた | 対応済み | `MasterController` を分離。後続で `master` パッケージへ移動 |
| DR-R-007 | テスト可読性 | Low | 正常系の登録、取得、編集、提出、提出後編集不可が1テストにまとまりすぎていた | 対応済み | `DailyReportControllerTest` の正常系テストを目的別に分割 |
| DR-R-008 | 可読性 | Low | `DailyReportResponse.from` が長く、recordの位置引数対応を追いにくかった | 対応済み | `WorkMinuteSummary`、`workItemResponses` に分割 |
| DR-T-001 | テスト不足 | Medium | AM_OFF、PM_OFF のAPIテストが不足していた | 保留 | DBマスタ化後も候補として継続。`日報登録編集_単体テストケースレビュー.md` |
| DR-T-002 | テスト不足 | Medium | 作業時間1分、備考1000/1001文字境界のテストが不足していた | 保留 | 境界値テスト追加候補。`日報登録編集_単体テストケースレビュー.md` |
| DR-T-003 | テスト不足 | Medium | 他社員の日報参照・編集不可の明示テストが不足していた | 保留 | 複数社員の初期データ整備後に追加候補。`日報登録編集_受入条件レビュー.md` |
| DR-T-004 | テスト不足 | Low | PUTでの入力チェック代表ケースが登録APIに比べて薄かった | 保留 | 編集APIの異常系追加候補。`日報登録編集_受入条件レビュー.md` |
| DR-T-005 | テスト不足 | Low | 再提出後の日報が編集不可であることの明示テストが不足していた | 保留 | 再提出API後の状態遷移テスト追加候補。`日報登録編集_受入条件レビュー.md` |
| DR-S-001 | セキュリティ | Medium | 提出・再提出時に保存済み日報の入力整合性を再検証していなかった | 対応済み | `TimeRules.validateStoredReport` を追加し、`submit` / `resubmit` で実行 |
| DR-S-002 | セキュリティ | Medium | CSRFなし登録APIのテストが不足していた | 対応済み | `createRequiresCsrfToken` を追加 |
| DR-S-003 | セキュリティ | High | `GET /api/daily-reports/{reportId}` で上長の承認対象グループ制限が未実装で、上長が任意の日報IDを知っている場合に範囲外の日報詳細を参照できる | 対応済み | `DailyReportService#get` に社員本人、上長担当グループ、管理者全件の詳細参照認可を追加。`DailyReportControllerTest.managerCanGetOnlyPermittedGroupReportDetail` を追加し、Oracle 実機接続のバックエンドテスト全体で確認。`日報カレンダー一覧_セキュリティレビュー.md` |
| DR-S-004 | セキュリティ | Low | CookieのSecure / SameSite属性は本番相当環境での確認が必要 | 保留 | 環境設定確認タスクとして継続。`日報登録編集_セキュリティレビュー.md` |
| DR-C-001 | 共通化 | Medium | 日報追加時にAPIクライアント、E2Eモック、ログイン画面部品の共通化確認が後追いになった | 対応済み | `shared/apiClient.ts`、`e2e/support`、`auth/LoginForm.tsx` へ整理 |
| DR-C-002 | 共通化 | Medium | MockMvcログイン補助とCSRF補助がテストクラス間で重複している | 保留 | `support` パッケージで共通化候補。`共通化チェック観点_2026-06-30.md` |
| DR-C-003 | 共通化 | Low | Oracleテスト実行時の環境変数設定が手動で、実行手順がぶれやすい | 対応済み | `backend/config/oracle-test.example.properties`、`backend/scripts/test-oracle.ps1`、`backend/scripts/test-oracle.cmd` を追加。実資格情報は Git 管理外の `backend/config/oracle-test.properties` に退避。`日報カレンダー一覧_作業記録.md` |
| DR-C-004 | 起動確認 | Medium | `vite preview` で起動した場合、`/api` がバックエンドへ転送されずログインAPIが404になっていた | 対応済み | `vite.config.ts` に dev/preview 共通の `/api` proxy を追加 |
| DR-F-001 | ファイル配置 | Medium | フロントエンドの `src` 直下にログイン、日報、共通部品が混在していた | 対応済み | `src/app`、`src/auth`、`src/dailyReport`、`src/shared` に分割 |
| DR-F-002 | ファイル配置 | Medium | `ApprovalStatus` が日報パッケージ配下にあり、日報専用の状態に見えた | 対応済み | `workflow/ApprovalStatus.java` へ移動 |
| DR-F-003 | ファイル配置 | Medium | `MasterController` / `MasterDataRepository` が日報パッケージ配下にあり、マスタが日報専用に見えた | 対応済み | `master` パッケージへ移動 |
| DR-F-004 | ファイル配置 | Medium | 日報パッケージにRequest/Response DTO、Entity、Repository、Service、業務ルールが同居しており、読む目的別に探しにくい | 対応済み | `report.dto` パッケージを追加し、`DailyReportRequest` / `DailyReportResponse` / `DailyReportSummaryResponse` / `SubmitResponse` を移動 |
| DR-F-005 | ファイル配置 | Low | フロントエンドの `shared/types.ts` に機能固有DTOが増えると、共通型と機能型の境界が曖昧になる | 対応済み | `auth/types.ts` と `dailyReport/types.ts` を追加し、`shared/types.ts` は横断の `ApiError` のみに整理 |
| DR-DB-001 | DB設計 | High | 休日区分などの業務区分がJava enum/static定義で、DBマスタとして管理されていなかった | 対応済み | `holiday_types`、`projects`、`work_categories`、`break_types`、`break_type_periods`、`work_time_types` を追加 |
| DR-DB-002 | DB設計 | Medium | 日報・明細テーブルからマスタへの外部キー参照が不足していた | 対応済み | DDLにマスタ参照FKを追加 |
| DR-DB-003 | DB設計 | Medium | 手動DB構築時にマスタ初期データ投入手順が分かれていなかった | 対応済み | `seed-master-data.sql` を追加 |
| DR-DB-004 | DB運用 | Medium | 既存Oracle環境に旧 `holiday_type` CHECK制約が残る場合のマイグレーション手順が未整理 | 保留 | 既存環境向け差分DDLまたはマイグレーション方針の整理が必要 |
| DR-DB-005 | DB運用 | Low | 既存マスタテーブルがあるOracle環境で初期化時に `ORA-00955` が発生した | 対応済み | `SQLException#getErrorCode() == 955` で既存テーブルを判定 |
| FIND-DBMSG-001 | DB設計・表示文言 | Medium | グループAPIは設計書に定義されているが物理DDL・Repository・Controllerが未実装で、グループ名称が利用者データと初期データへ分散していた。またAPIエラーとFrontend表示文言がソースへ重複していた | 対応済み | `groups`、`message_catalog`、Repository、API、Frontend共有Context、DB seedを実装。状態・ロール・検証ロジックはソース正として維持。**標準化候補**: 安定キーとDB表示本文の分離を共通のDBマスタ設計・実装後レビュー観点へ反映する。`区分メッセージDB化_実装後レビュー.md` |
| FIND-DBMSG-002 | 認証認可・フェイルクローズ | High | 上長権限Repositoryが未注入の場合に、グループAPIが全グループを返す実装となっており、設定不備が権限範囲の拡大につながる可能性があった | 対応済み | `MasterController` を管理者以外は権限Repository必須とし、未注入時は空配列を返すfail-closedへ修正。`MasterControllerUnitTest.managerWithoutPermissionRepositoryReceivesNoGroups`を追加。**既存観点で対応**: 認証認可・安全側応答の既存レビュー観点で再発検出可能 |
| FIND-DBMSG-003 | DB seed・トレーサビリティ | Medium | MessageCatalogDefaultsに追加したキーがOracle seedへ全件反映されているかを機械的に確認する契約がなく、初回確認で2キーのseed漏れが判明した | 対応済み | `seed-master-data.sql`へ不足2キーを追加し、最終確認でソース87キー・seed87キーの一致を確認。**標準化候補**: DBマスタ追加時のソース既定値・DataInitializer・seed SQLの三者同期確認を実装後レビューのDB整合性観点へ追加する。 |
| FIND-DBMSG-004 | テスト・E2E再現性 | Medium | E2Eが実行日の当月に依存し、休日区分モックに業務ルール属性がなく、メッセージDB化後の状態表示・再提出確認で既存契約を再現できなかった | 対応済み | `daily-report.spec.ts`で対象月を明示し、`dailyReportApprovalMocks.ts`へ`requiresWorkTime`/`allowsWorkItems`を追加。E2E17件成功。**既存観点で対応**: テストfixture再現性・期待結果・日付境界の既存観点で検出可能 |
| FIND-DBMSG-005 | 静的解析・防御性 | Medium | 新規メッセージ実装でSpotBugsの未使用private method、Map外部公開、Spring依存注入、TTL検証コンストラクタ指摘が検出された | 対応済み | 未使用オーバーロード削除、レスポンスMap防御コピー、SpotBugs除外へSpring依存と検証コンストラクタの理由を記録。Spotless/Checkstyle/SpotBugs/PMD成功。**既存観点で対応**: Full静的解析で検出可能 |
| FIND-DBMSG-006 | Oracle integration・Spring DI | High | Oracle接続後のintegration実行で、`MessageCatalogService` に複数コンストラクタがあるがSpringが使用するコンストラクタを選択できず、`No default constructor found` でApplicationContext起動に失敗した | 対応済み | `backend/src/main/java/com/example/dailyreport/master/MessageCatalogService.java` の本番用コンストラクタへ`@Autowired`を付与し、`MessageCatalogServiceTest.springCanConstructServiceWithRepositoryConstructor`を追加。Oracle integration 154件、Backend coverage 161件、E2EOracleを再実行して成功。**標準化判定: 既存観点で対応**。DI構成とOracle実起動の結合テストで再発検出可能 |

## 運用ルール

- レビューで問題を検出した場合は、作業記録だけでなくこの指摘一覧にも追記する。
- 対応済みにする場合は、対応ファイルまたは対応記録先を必ず記載する。
- 保留にする場合は、保留理由と再確認タイミングを記載する。
- 同じ問題を複数資料に書く場合でも、指摘IDはこの一覧を正とする。

## 2026-08-02 PMD複雑度ルール適用 追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-Q-001 | 静的解析・可読性 | Medium | `RequestContext.useCaseForPath`、`TimeRules.validateStoredReport`などに認知的複雑度、循環的複雑度、NPath複雑度の既存違反があり、新規違反も自動検出できなかった | 対応済み | `backend/config/pmd.xml`へPMD 3ルールを追加し、`RequestContext`、保存済み日報検証、検索期間検証を整理。`scripts/pmd.tests.ps1`、`PMD複雑度ルール適用_2026-08-02.md`。標準化候補として品質ゲート・標準資料へ反映 |
| DR-Q-002 | 静的解析・可読性 | Low | Checkstyleで波括弧、boolean表現、`@Override`漏れを検査しておらず、PMDの複雑度ルールだけでは可読性の一部が自動検出されなかった | 対応済み | PMDへ`ControlStatementBraces`、`SimplifyBooleanExpressions`、`SimplifyBooleanReturns`、`MissingOverride`を追加。標準化候補としてPMD契約テストと品質ゲートへ反映 |

## 2026-07-01 追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| FIND-LEGACY-UI-001 | 表示文言 | Medium | 日報登録・編集画面に `Daily Report`、`Register daily report`、`Work items`、`DRAFT`、`EMPLOYEE` など英語表示が残っていた。APIエラー文言とマスタ初期データにも英語があり、画面上に英語が再表示される可能性があった。 | 対応済み | フロントの静的文言、ロール・状態表示、APIエラーフォールバック、E2E/単体テスト期待値を日本語化。バックエンドの入力チェック・マスタ存在チェックのエラーメッセージ、`DataInitializer`、Oracleマスタ投入SQLも日本語化。 |

## 2026-07-02 追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-F-006 | ファイル配置 | Low | 1用途の小さな補助ファイルが複数あり、機能を読む際にファイル移動が増えていた。特に `loginUi.ts`、`routePolicy.ts`、`shared/types.ts`、`CalculatedWorkTime.java` は単独ファイルにするほどの独立性が低かった。 | 対応済み | `LOGIN_FORM_TEXT` は `LoginForm.tsx`、初期画面ルールは `authApi.ts`、`ApiError` は `apiClient.ts`、`CalculatedWorkTime` は `TimeRules` 内部recordへ統合した。 |
| DR-F-007 | ファイル配置 | Medium | 日報機能の `report` 直下に Controller、Entity、Repository、計算ロジックが混在しており、人が日報バックエンドを読む際の入口が分かりにくかった。 | 対応済み | `DailyReportService` は `report` 直下に残し、`controller`、`entity`、`repository`、`logic`、`dto` のサブパッケージへ分類した。 |
| DR-F-008 | ファイル配置 | Low | Entityクラス名が `DailyReport` / `DailyReportWorkItem` のままで、Entityであることがファイル名から分かりにくかった。またRepositoryはEntityと同じ永続化関心として近い場所に置く方が探しやすかった。 | 対応済み | `DailyReportEntity` / `DailyReportWorkItemEntity` に改名し、`DailyReportRepository` を `report.entity` 配下へ移動した。 |
| DR-R-009 | 可読性 | Low | 実装ソースの冒頭説明や、分岐・業務判断・計算処理のコメントが少なく、初見で役割や意図を追いにくかった。 | 対応済み | バックエンド/フロントエンドの実装ソース先頭に役割コメントを追加し、認証、CSRF、日報状態遷移、勤務時間計算、休日区分分岐、画面状態更新などに意図コメントを追加した。 |

## 2026-07-09 追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-005 | ドキュメント運用 | Medium | ユーザからの追加指摘「DB接続情報が共通化されていないのでは」を作業記録には反映したが、指摘一覧へ記載するかの判定が即時に行われていなかった。 | 対応済み | AGENTS.md の運用ルールに従い、`DR-C-003` を対応済みに更新し、本指摘 `DR-C-005` を追加。以後、ユーザ追加指摘を受けた時点で「指摘一覧へ載せるか」「規約・チェック表へ反映するか」を明示判定する。 |
| DR-C-006 | ドキュメント運用 | Medium | ユーザ追加指摘の規約・チェック表・レビュー観点への反映要否を、指摘一覧更新後に明示確認していなかった。 | 対応済み | `実装後レビュー表.md` にユーザ追加指摘の判定欄を追加し、`開発フロー.md` の基本原則、Record手順、Recordゲートへ反映した。 |
| DR-C-007 | ドキュメント運用 | Medium | 旧フェーズプロンプトでコード生成後に通常の実装後レビュー依頼テンプレートがなく、セキュリティレビューだけが後続レビューとして目立つ構成になっていた。 | 対応済み | `projectfoundation-review-ja` Skillへ実装後レビュー、指摘対応、標準化判定、最終品質ゲートを統合し、仕様整合、テスト不足、可読性、共通化、記録更新、ユーザ追加指摘の反映要否を確認する流れにした。 |
| DR-C-008 | ドキュメント運用 | Medium | 実装後レビューや独立調査で、確認観点が複数ある場合のサブエージェント利用判断がプロンプトテンプレートに明示されていなかった。 | 対応済み | 2026-07-11 方針変更により、サブエージェント利用記載はレビューや独立調査での利用判断に限定した。`projectfoundation-review-ja`、`開発フロー.md` |
| DR-C-009 | ドキュメント運用 | Low | 旧フェーズプロンプトの一部で、brainstorming、writing-plans、test-driven-development、dispatching-parallel-agents などの superpowers 利用判断が明示されていなかった。 | 対応済み | `projectfoundation-preflight-ja` と `projectfoundation-review-ja` Skillへ、設計、曖昧点、テスト設計、独立レビュー、指摘対応、原因分析、影響範囲ごとのsuperpowers利用判断を統合した。 |
| DR-C-010 | ドキュメント運用 | Low | レビュー依頼とレビュー指摘対応で、superpowers:requesting-code-review と superpowers:receiving-code-review の利用判断がプロンプトテンプレートに明示されていなかった。 | 対応済み | `projectfoundation-review-ja` Skillへ requesting-code-review と receiving-code-review の利用判断を統合した。 |
| DR-C-011 | ファイル配置 | Medium | チーム並列開発を想定した場合、別機能が同一のController、Service、Repository、テストファイルを更新し続ける構成を避ける観点が、実装前チェックやレビュー観点で明示されていなかった。 | 対応済み | `ディレクトリ構成ルール.md` に並列開発しやすい分割方針を追加し、`実装前チェック表.md`、`実装後レビュー表.md`、`projectfoundation-preflight-ja`、`projectfoundation-review-ja` にファイル衝突リスク確認を追加した。 |
| DR-C-012 | ドキュメント運用 | Medium | 標準化資料に旧フェーズプロンプトと `AI活用開発手順書.md`、`開発フロー.md` など役割が近い資料が並存し、さらに `チーム開発_` 接頭辞の有無でファイル名が分かりにくくなっていた。 | 対応済み | 正本をProjectFoundation Skill、`開発フロー.md`、`実装前チェック表.md`、`実装後レビュー表.md`、`AI専門レビュー用プロンプト定義.md`へ整理し、標準化資料のファイル名から `チーム開発_` 接頭辞を外した。旧資料は `旧資料/` に退避し、`標準化資料一覧.md` と`標準化資料整理_2026-07-10.md`に整理結果を記録した。旧フェーズプロンプトはSkillへ統合後に削除した。 |

## 2026-07-09 日報カレンダー・一覧画面 追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DRL-R-001 | 入力チェック | Medium | 対象年月を空にした場合、画面側で不正な日付範囲を作り得る。日付範囲不正時は検索前に止める方針に対して、対象年月クリア時と日付形式不正の補助チェックが不足している。 | 対応済み | `dailyReportSearch.ts` で対象年月・日付形式の検証を追加し、対象年月空時は日付範囲を空にして検索前バリデーションで停止する。Vitest追加済み。 |
| DRL-R-002 | 仕様整合/UI | Medium | 上長・管理者で同日に複数社員の日報がある場合、カレンダーは先頭1件だけを表示・色付けするため、同日の他の日報や異なる承認状態が表現されない。 | 保留 | `日報カレンダー一覧_実装後レビュー.md`。カレンダーを日別件数・状態サマリにするか、社員ロールのみ詳細表示にするか仕様確認後に対応する。 |
| DRL-S-001 | セキュリティ | Medium | 一覧APIが対象期間必須ではあるが、検索期間の上限や件数上限がなく、広すぎる日付範囲で大量検索が可能。 | 対応済み | API側で検索期間を366日以内に制限し、超過時400になるMockMvcテストを追加。 |
| DRL-S-002 | セキュリティ | Medium | `dateFrom=abc` や `status=INVALID` など型変換不能な検索条件が、共通エラー形式ではなくSpring既定エラーになる可能性がある。 | 対応済み | `ApiExceptionHandler` で型変換例外を共通の `VALIDATION_ERROR` と固定文言に変換。不正日付・不正承認状態のMockMvcテストを追加。 |
| DRL-S-003 | セキュリティ | Medium | ログアウトAPIが403/500でもフロントがログアウト済み表示に進み、サーバ側セッションが残ったまま利用者がログアウトできたと誤認する可能性がある。 | 対応済み | `postNoContentWithCsrf` でHTTP失敗を例外化し、`App.handleLogout` は成功時のみログイン状態を破棄する。フロント単体テスト追加済み。 |
| DRL-S-004 | セキュリティ | Medium | 一覧取得後にセッション期限切れや別タブログアウトが発生した場合、401を受けても既存の一覧データや個人情報が画面に残る。 | 対応済み | 一覧検索401時に一覧データをクリアし、親コンポーネントでログイン状態破棄とログイン画面遷移を行う。E2E追加済み。 |
| DRL-S-005 | テスト不足 | Low | 日報一覧APIの未ログイン拒否、検索時の他者社員ID指定、CSRFヘッダー退行を直接検知するテストが不足している。 | 対応済み | 一覧・詳細の未ログイン、社員検索の他者ID無効化、CSRFヘッダー退行、401時画面遷移の回帰テストを追加。Oracle 実機接続のバックエンドテスト全体で確認。 |
| DRL-T-001 | テスト不足 | Medium | 日報一覧APIの条件検索と並び順について、テストケース資料の一部ケースに対応する直接テストが不足している。 | 対応済み | `DailyReportControllerTest` に上長の担当外 `groupId` 指定、管理者の複数社員横断取得、`groupId` / `employeeId` 絞り込み、日付・社員ID昇順のMockMvcテストを追加。Oracle 実機接続のバックエンド全体テストで確認。 |
| DRL-T-002 | テスト不足 | Medium | 日報カレンダー・一覧画面のロール別検索条件表示、承認状態色、一覧主要列、0件表示の自動テストが不足している。 | 対応済み | Playwright E2Eに社員のグループID欄非表示、上長のグループID欄表示、状態別カレンダーclass、一覧列、空状態の確認を追加。 |
| DRL-T-003 | テスト不足 | Medium | ログアウト失敗時にAppのログイン状態を維持することを、画面状態まで確認する自動テストが不足している。 | 対応済み | Playwright E2Eに `/api/auth/logout` 失敗時のログイン状態維持とエラー表示確認を追加。 |
| DRL-T-004 | テストケース資料 | Low | 設計書上の受入条件のうち、今回対象外・保留にした日報詳細/登録/編集遷移などがテストケース資料で明示されていない。 | 対応済み | `日報カレンダー一覧_テストケース.md` に対象外・保留セクションを追加し、遷移、同日複数日報表示、承認者名/承認日時の再確認条件を明記。 |

## 2026-07-13 追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-F-009 | ファイル配置・責務分離 | Medium | フロントの日報検索テストが機能実装配下にあり、バックエンドの日報 Controller、Service、テストクラスにも複数ユースケースが集中していた | 対応済み | フロントテストを frontend/test へ移動し、バックエンドを検索・登録更新・提出・マスタの Controller / Service / テストへ分割。scripts/check-test-layout.ps1、DailyReportSeparationTest、標準化資料を追加。 |

## 2026-07-13 コードコメント・検証入口追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-R-010 | 可読性・コメント | Medium | 本体コードのコメントがファイル役割や処理手順を説明するHowに偏り、コードだけでは分からない採用方式・除外理由のWhy notを表していなかった | 対応済み | ファイル役割・How重複コメントを削除し、セッション、CSRF、状態遷移、業務整合性、Oracle再実行性などの判断理由を`Why not:`へ移行。標準資料、AIプロンプト、レビューskill、`コードコメント運用強化_2026-07-13.md` を更新。 |
| DR-C-013 | テスト運用・共通化 | Low | Oracle接続情報は `backend/config/oracle-test.properties` と `backend/scripts/test-oracle.cmd` へ共通化済みだったが、直接 `mvn.cmd` を実行する確認手順が残り、共通入口を bypass して `ORA-01017`になった | 対応済み | `テスト・静的解析チェック表.md` と実装計画のバックエンド実行コマンドを `backend/scripts/test-oracle.cmd -q test` へ統一。共通入口で再実行し終了コード0を確認。`コードコメント運用強化_2026-07-13.md` |
| DR-R-011 | 可読性・コメント | Medium | コメントをWhy notだけに限定すると、命名だけでは共有できない分岐の優先順位、アルゴリズム、処理順、重要な副作用のHowが失われる | 対応済み | 責務・境界・契約の冒頭コメント、誤読防止の`How:`、不採用方式の`Why not:`を分けるルールへ修正し、`TimeRules`、認証・CSRF、日報フォームなどへHowコメントを復元。標準資料、設計書、計画、作業記録を更新。 |
| DR-R-012 | 可読性・コメント | Medium | メソッド名だけでは責務、入力・出力、状態変更、例外・分岐の契約が分からず、実装本体を読まないと処理の目的を把握できないメソッド・関数が残っていた | 対応済み | 公開API、状態変更、DBアクセス、外部通信、例外・複数分岐を持つJavaメソッドとTypeScript関数の直前へ日本語コメントを追加。単純なアクセサ・一行委譲は規約上の例外として整理し、`開発フロー.md`、`実装後レビュー表.md`、プロンプト、レビューskillへ反映。 |
| DR-R-013 | 可読性・コメント | Medium | `if`分岐のコメントが`Why not`中心で、条件に応じた処理手順、優先順位、早期return、状態変更、許可・拒否の結果を実装本体から読み取らないと把握できない箇所が残っていた | 対応済み | バックエンド・フロントエンドの主要な`if`/`else`分岐へ日本語の`How:`を追加し、`Why not:`は不採用方式の理由に限定する基準を`開発フロー.md`、実装前後チェック表、プロンプト、レビューskillへ反映。テストはユーザー指定により再実施せず、差分と静的確認を記録。 |

## 2026-07-14 品質ゲート追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-014 | テスト運用・品質ゲート | High | `main`のGit hookランチャーは存在したが、リポジトリ直下にLefthook設定と共通Runnerがなく、コミット・プッシュ時に検査を実行せず成功扱いになっていた | 対応済み | `lefthook.yml`、`scripts/check.ps1`、Bootstrap、Doctor、GitHub Actions Quality/Security/Oracleをリポジトリ管理へ追加。設定・ツール不足は非0終了とし、`品質ゲート運用.md`を正本として運用を統一。 |
| DR-C-015 | テスト運用・環境差 | Medium | worktreeごとに品質設定の有無が異なり、品質ハーネスworktreeではQuick/Fullが動く一方、`main`では空振りしていた | 対応済み | 品質設定を`main`へ取り込み、Node/npm、Java、Maven、Lefthook、Gitleaks、Playwrightの固定版を`tool-versions.psd1`とDoctorで確認する構成に変更。現在ホストのNode/npm差異はDoctorで検出する。 |
| DR-C-016 | 品質ゲート検証 | Medium | 品質ゲート導入直後はMarkdownlint、Maven Wrapper、Gitleaksが未導入で、設定があっても実行環境不足により失敗した | 対応済み | `scripts/bootstrap.ps1`で依存関係と固定Gitleaksを導入し、Quickを再実行。Markdownlint、Spotless、Gitleaks、staged空白・生成物検査がすべて成功した。Full、CI、Oracle実機確認は別環境条件を確認して実施する。 |

## 2026-07-15 テストケース品質様式追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-017 | テスト運用・ドキュメント | High | 受入条件、テストケース、過去障害・レビュー指摘、実テストの間に安定した相互参照項目がなく、期待結果レビューと専門レビューの記録形式も統一されていなかった | 対応済み | 受入条件に `COMMON-AC` / `F-xxx-AC` のIDを付番し、テストケース様式に受入条件ID、過去障害・指摘ID、実テストID、期待結果、専門レビュー欄を追加。障害様式に原因・未検出理由・再発防止反映先・トレーサビリティ状態を追加し、プロンプト、レビュー観点、欠陥CSVを更新した。`テストケース品質様式強化_2026-07-15.md` |

## 2026-07-15 ロガー・実Backend/Oracle E2E追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DRL-OBS-001 | 可観測性 | High | Backendログだけでは、どの機能・ユースケースがいつ処理され、成功したか失敗したかをリクエスト単位で追跡できなかった。 | 対応済み | `RequestIdFilter`、`RequestMetadataInterceptor`、MDC、`X-Request-Id`、HTTPステータス・処理時間ログを追加。`backend/src/test/java/com/example/dailyreport/observability`で相関IDと後始末を検証。 |
| DRL-OBS-002 | エラー処理 | High | 業務エラー、認証拒否、CSRF/認可失敗、予期しないシステム例外で、画面向け応答とサーバー側調査ログの契約が統一されていなかった。 | 対応済み | `ApiExceptionHandler`の共通`requestId`/500汎用応答、`SecurityConfig`の401/403 JSON、WARN/ERRORログを追加。秘密値・例外詳細をレスポンスへ出さないテストを追加。 |
| DRL-OBS-003 | Frontend障害追跡 | Medium | FrontendがBackendの相関IDを保持せず、500やネットワーク障害時にユーザー操作とBackendログを結び付けにくかった。 | 対応済み | `frontend/src/shared/apiClient.ts`でJSON/headerの`requestId`を`ApiError`へ保持し、500以上・ネットワーク障害だけをパス等の安全な項目で診断出力。`frontend/test/apiClient.test.ts`を追加。 |
| DRL-OBS-004 | テスト不足 | High | 既存のPlaywright E2EはAPIをmockしており、実BackendとOracle接続、DB永続化、再読込、Backendログを同時に確認できなかった。 | 対応済み | `frontend/e2e/oracle-daily-report.oracle.spec.ts`、`backend/scripts/test-e2e-oracle.ps1`、Oracle cleanup/verify SQL、`oracle.yml / oracle-e2e`を追加。ローカルTEST Oracleで実行し、ログ相関、重複409、`PENDING`保存、cleanup完了を確認。 |
| DRL-OBS-005 | 運用・性能 | Medium | リクエスト開始/完了を常時詳細出力すると数百人利用時のログ量・I/O負荷が増えるため、本番とE2Eの出力レベルを分ける必要があった。 | 対応済み | 本番はroot `WARN`・アプリ`INFO`、詳細リクエストは`DEBUG`、E2Eは`application-e2e.yml`でDEBUG＋ファイル出力に設定。`セキュリティ規約.md`と`品質ゲート運用.md`へ運用ルールを反映。 |

| DRL-OBS-006 | Oracle E2E安全性 | High | 実Oracle E2Eでcleanupを実行する前に、DB名・サービス名・セッションユーザーの一致を確認し、専用E2Eユーザーと保存結果を検証する必要があった。 | 対応済み | `oracle-test-helpers.ps1`のidentity guard、`seed-user-test.sql`のrunner実行、`e2e-verify.sql`、`DataInitializer`の不足ユーザー単位seedを追加。 |
| DRL-OBS-007 | ログ機密性 | High | 想定外例外をThrowableのままERRORログへ渡すと、例外メッセージにパスワード等が含まれた場合にサーバーログへ漏れる可能性があった。 | 対応済み | `ExceptionLog`で例外型とスタック位置だけを抽出し、`ApiExceptionHandler`と`RequestIdFilter`から例外メッセージを出力しない構成へ変更。 |
| DRL-OBS-008 | エラー分類・追跡 | Medium | Security拒否時にMVCのHandlerInterceptorが実行されず機能/ユースケースが`UNKNOWN`になり、既知のJSON形式不正も500扱いになる可能性があった。 | 対応済み | `RequestContext`のパス解決をFilterへ追加し、JSON形式不正・必須パラメータ不足を共通400/WARNへ分類。 |
| DRL-OBS-009 | Oracle CI運用 | Medium | 同じTEST Oracleを使うIntegration、Coverage、実E2E jobが並列実行されるとseed/cleanupが競合する可能性があった。また実ログインを含むtrace成果物に認証情報が残る可能性があった。 | 対応済み | Oracle jobへ同一concurrency groupを設定し、Oracle E2Eのtrace/video/screenshotを無効化。 |
| DRL-OBS-010 | ドキュメント運用・可観測性 | Medium | ロガーを個別機能の実装詳細として扱うと、システム共通のイベント網羅、相関、結果の正確性、負荷、機密性、エラー終端、検証方法の確認がレビューや実装前計画から漏れる可能性があった。 | 対応済み | `実装前チェック表.md`、`実装後レビュー表.md`、`セキュリティ規約.md`へ共通ロガー観点を追加。実装前は前者の「ログ設計」と後者のログ節を主資料とし、対応内容を本作業記録にも記載。 |

## 2026-07-15 ログイン画面テストケース品質レビュー

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-018 | テスト運用・トレーサビリティ | High | ログインの実テストには`LoginForm.test.tsx`の画面ケースとログアウト失敗ケースが存在するが、旧ケース台帳に記載されず、受入条件・過去障害・期待結果レビュー・専門レビューとの対応も追跡できなかった。 | 対応済み | `ログイン機能_テストケース.md`へ`FE-AUTH-009`～`FE-AUTH-012`と品質追跡表を追加。`ログイン機能_テストケース品質レビュー_2026-07-15.md`で受入条件、`D-LOGIN`、実テスト、レビューIDを相互参照。 |
| DR-C-019 | テスト不足・期待結果 | Medium | `App`の起動時未認証切替、ログイン成功後のロール別画面・URL、ログアウト成功・失敗時の画面状態、ログインAPIエラー表示を統合層で確認していなかった。APIと部品単体の確認だけでは画面受入条件の一部を保証できない。 | 対応済み | `App.test.tsx` 6件と`LoginForm.test.tsx` 1件を追加し、`FE-AUTH-013`～`FE-AUTH-017`として記録。`D-LOGIN-021`、`LOGIN-APP-001`～`LOGIN-APP-003`、`F-001-AC-002`、`F-001-AC-003`、`F-001-AC-004`、`F-001-AC-005`、`F-001-AC-006`を再確認した。 |

## 2026-07-17 Markdownlint是正追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-020 | テスト運用・ドキュメント | Medium | Markdownlintの初回全体実行で、ローカルツール・worktree・依存物まで検査対象となり、プロジェクト資料にも重複見出し、空行、表整形、コードフェンスの違反が残っていた。 | 対応済み | `.markdownlint-cli2.jsonc` に `gitignore: true` と `MD024.siblings_only` を設定し、対象Markdownの自動・手動整形を実施した。作業記録追加後の `npm.cmd run lint:markdown` は81ファイル、0件となった。詳細は `Markdownlint是正_2026-07-17.md` に記録した。 |

## 2026-07-18 品質ゲート再確認追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-021 | Security・依存監査 | High | Security workflowのDependency Auditで、frontendの`npm audit`が`esbuild`のmoderate 1件と`vite`のhigh 1件を検出して失敗した。backendのOWASP Dependency-Checkも初回NVD更新中にH2/MVStore読み取りエラーと接続プール初期化エラーを起こし、依存監査全体が失敗した。 | 保留 | 依存更新はViteのbreaking changeを伴う可能性があるため自動適用しない。frontendのVite/esbuild更新可否を互換性確認後に判断し、backendはDependency-CheckのNVDデータ取得・キャッシュ方式を整備して再実行する。Security run `29623533201`、作業記録 `カバレッジ閾値強化_2026-07-17.md`。 |
| DR-C-022 | Oracle CI運用 | High | main merge後のOracle workflowがpendingのままとなった。self-hosted runnerが0台で、`oracle-test` environmentのsecret / variableも未設定のため、実Oracle、Backend Coverage、Oracle E2Eを実行できない。 | 保留 | `oracle-test` environmentをprotected branch限定へ設定済み。専用runner、`DAILY_REPORT_DB_URL`・ユーザー・パスワード・expected identity、test schemaを用意し、mainからOracle 3 jobを再実行する。Oracleをrequired status checkへ追加するのは、その実行が安定してからとする。 |
| DR-C-023 | 品質ゲート・実行時間 | Medium | Dependency Auditにjob timeoutがなく、初回NVD更新で約15分実行してから失敗するまで、無期限実行を防ぐ設定がなかった。 | 対応済み | `.github/workflows/security.yml`の`Dependency Audit`へ`timeout-minutes: 30`を追加。キャッシュ済み環境・NVD更新失敗時の再実行手順は`品質ゲート運用.md`に記載した。 |

## 2026-07-18 pre-push実行時間追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-C-024 | 品質ゲート・実行時間 | Medium | `pre-push`でFullを実行すると、pushごとにFrontend / Backendのテスト・build・静的解析がCIと重複し、ローカルpushの待ち時間が長くなる。 | 対応済み | `scripts/check.ps1`へpush標準入力と差分を扱う`PrePush` Modeを追加し、`lefthook.yml`のpre-pushを差分中心の検査へ変更した。Fullとrequired CIは維持する。`品質ゲート運用.md`、`カバレッジ閾値強化_2026-07-17.md` |
| DR-C-025 | 品質ゲート・差分対象 | Medium | `pre-push`を単純にQuickへ置き換えると、Quickのstaged対象前提により、既にcommitされたpush対象差分を検査できない可能性がある。 | 対応済み | Lefthookのstdinからlocal / remote refとSHAを解析し、通常更新・新規branch・削除refを分岐する専用`PrePush`契約テストと実行処理を追加した。`scripts/pre-push.tests.ps1`、`scripts/check.ps1` |

## 2026-07-18 S-004実装前レビュー追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| FIND-DRE-001 | 設計・状態遷移 | High | `REJECTED` の「提出」と「再提出」のAPI入口が設計文言上曖昧で、初回提出APIを誤って呼ぶ余地があった。 | 対応済み | `REJECTED` は保存後に再提出APIだけを呼ぶ仕様へ設計書、正本ケース、実装計画へ反映。`REV-DRE-DESIGN-001`、TC-DRE-009。 |
| FIND-DRE-002 | トレーサビリティ | High | 旧 `DR-*` ケースと指摘IDの衝突、AC/TC/RT/FINDの相互参照不足により、ケースと実テストの対応を一意に追跡できなかった。 | 対応済み | 旧指摘IDを `FIND-LEGACY-*` へ移行し、今回分を `TC-DRE-*`、`RT-DRE-*`、`FIND-DRE-*` として正本対応表へ追加。`REV-DRE-TRACE-001`。 |
| FIND-DRE-003 | 安全側UI・テスト不足 | High | 編集詳細の読込中・読込失敗後に新規フォーム初期値で保存でき、`PENDING`/`APPROVED` の全操作も有効だった。 | 対応済み | `DailyReportForm.tsx`でloading/failed/PENDING/APPROVEDの全操作と保存処理を停止。`App.test.tsx`、`daily-report.spec.ts`で全control disabled、状態説明、PUT/POST 0件を確認。`REV-DRE-TASK3-02`、`REV-DRE-DESIGN-POST`、`REV-DRE-TEST-POST`。 |
| FIND-DRE-004 | 認証認可・テスト不足 | High | 他社員の日報の更新・提出・再提出を実在データで確認するケースと複数社員seed条件が不足していた。 | 保留 | `TC-DRE-016`、`RT-DRE-API-003`へ追加。複数社員seedと実Backend/Oracle環境が揃った時に再確認する。 |
| FIND-DRE-005 | APIテスト不足 | Medium | PUT入力不備・編集日付重複・状態別提出拒否の既存APIテストと、AC/TC/RT対応が不足していた。 | 保留 | `TC-DRE-017`～`TC-DRE-018`、`RT-DRE-API-004`～`005`へ追加。実装後レビューで既存テストの充足を再確認する。 |
| FIND-DRE-006 | 競合・標準化候補 | High | 更新と提出・承認の同時実行に対する楽観ロック、行ロック、条件付き更新が確認できず、状態競合の原子性を保証できない。 | 保留 | 今回はフロントの409表示と読込中・失敗後の安全側制御のみ実装。バックエンドの楽観ロック等を導入する時に再確認する。 |
| FIND-DRE-007 | E2E・対象範囲 | Medium | F-009-AC-012の提出→差戻し→修正→再提出完全E2Eは、F-008差戻し機能が今回対象外のため実行できない。 | 保留 | F-008実装後、実Backend/Oracle E2Eを実行する。`日報登録編集_テストケース品質レビュー_2026-07-18.md`。 |

## 2026-07-18 プロセス・Skill改善

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-PROC-001 | プロセス・テストケース | High | 正本テストケースと受入条件レビューが実装開始の停止条件になっておらず、仕様変更後のケース台帳も更新されていなかった。 | 対応済み | `projectfoundation-preflight-ja`、`実装前チェック表.md`、`テストケースレビュー観点.md`へ正本ケースレビューと受入条件レビューを必須化。再開時の更新対象を `プロセス改善_Skill・レビューゲート強化_2026-07-18.md` に記録。 |
| DR-PROC-002 | プロセス・専門レビュー | High | Full実行後に単一の汎用コードレビューを行ったため、観点別の独立レビューと、指摘修正後の最終ゲートを事前に設計できていなかった。 | 対応済み | `projectfoundation-review-ja`、`開発フロー.md`、`実装後レビュー表.md`、`AI専門レビュー用プロンプト定義.md`へ設計・テスト不足・期待結果・トレーサビリティ等の専門レビュー、統合レビュー、レビュー後Fullを必須化。 |
| DR-PROC-003 | トレーサビリティ・標準化 | High | 指摘と受入条件、テストケース、実テストの対応、標準化判定、反映先、保留理由、再確認条件が一貫した記録形式になっていなかった。 | 対応済み | AC/TC/RT/FINDのID名前空間、指摘ごとの標準化判定・反映先・再確認条件をSkill、標準資料、`実装後レビュー表.md`へ追加。今回の判断と再開条件を `プロセス改善_Skill・レビューゲート強化_2026-07-18.md` に記録。 |

## 2026-07-19 S-004実装後レビュー追記

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| FIND-DRE-008 | テスト不足・期待結果 | High | 状態別Unit/E2Eでcontrol配列が空でも`every([])`が成功し、セレクタ不備や画面要素消失を見逃す可能性があった。 | 対応済み | Unit/E2Eで対象control件数`>0`を先に確認し、E2Eは「日報編集」見出しを持つ編集パネルへ限定。`日報登録編集_実装後レビュー_2026-07-18.md`、`REV-DRE-TASK3-02`。 |
| FIND-DRE-009 | 仕様・テストケース | Medium | TC-DRE-008の提出日時が、APIで記録する要件と画面に表示する要件の語彙で混在していた。 | 対応済み | 設計書に表示要件がないため、正本TCを提出API/既存Backendテストによる記録確認へ修正し、画面表示は追加しない。受入条件レビュー、実装後レビュー記録。 |
| FIND-DRE-010 | テスト不足 | High | REJECTEDを下書き保存しただけでREJECTEDを維持する証拠が、再提出と同じテストに埋もれていた。 | 対応済み | `saves an existing rejected report without changing its rejected state`を追加し、PUTのみ・保存成功・REJECTED維持を確認。`App.test.tsx`、TC-DRE-003、`REV-DRE-TEST-POST`。 |
| FIND-DRE-011 | E2E・状態遷移 | High | DRAFT提出後の再読込でPENDING・編集不可になる状態遷移を、固定DRAFTモックでは検出できなかった。 | 対応済み | DRAFTモックを提出後PENDINGへ遷移する状態付きに変更し、既存DRAFT提出E2Eで再読込後の状態説明・全操作disabledを確認。`dailyReportMocks.ts`、`daily-report.spec.ts`、TC-DRE-011。 |
| FIND-DRE-012 | 安全側テスト | Medium | loading/failedのmutation 0件確認が作成POST/PUTだけで、提出・再提出を含む全変更系経路を明示していなかった。 | 対応済み | UnitでPUT/POST全件を0件とするアサーションを追加。TC-DRE-006～007、`REV-DRE-TEST-POST`。 |
| FIND-DRE-013 | E2E・トレーサビリティ | Medium | REJECTED E2Eが初回submit 0件・resubmit 1件だけで、PUTの存在・順序と差戻し日時表示を明示していなかった。 | 対応済み | PUT→resubmitの完全順序、コメント・差戻し者・差戻し日時、submit未呼出を確認。`daily-report.spec.ts`、TC-DRE-012、`REV-DRE-DESIGN-POST`。 |
| FIND-DRE-SEC-001 | セキュリティ・競合 | High | 同一日報の並行更新・提出に楽観ロックまたは条件付き更新がなく、状態遷移の原子性を保証できない。 | 既存FINDへ統合・保留 | `FIND-DRE-006`へ統合。S-004設計書の対象外判断を維持し、Backendへ`@Version`または条件付き更新を導入する時に再確認する。`REV-DRE-SEC-POST`。 |
| FIND-DRE-FINAL-001 | テスト不足 | Medium | REJECTED編集時のフォーム変更値がPUT bodyへ反映されることをテストで保証していなかった。 | 対応済み | `saves an existing rejected report without changing its rejected state`で備考変更とPUT bodyの`remarks`を確認。`REV-DRE-FINAL-02`。 |

## 2026-07-19 Oracle接続・共通runner再確認

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| FIND-DRE-014 | Oracleテスト運用 | High | ローカルにOracle接続情報は存在したが、強化後runnerが必須とする`TEST`環境・期待DB identity・DDL許可項目が未反映で、接続前に停止していた。 | 対応済み | Git管理外の`backend/config/oracle-test.properties`へ非機密の必須項目を追加。Oracle安全識別、`-Mode Oracle`、Backend/Oracle E2Eで再確認。 |
| FIND-DRE-015 | 品質ゲート・CMD | Medium | `backend/scripts/test-oracle.cmd`の括弧付き`echo`が`if (...)`ブロック内で解釈され、PowerShell実行前に`is was unexpected at this time.`で停止していた。 | 対応済み | エラーメッセージから括弧を除去し、`scripts/oracle-preflight.tests.ps1`でPowerShell設定検証まで到達する契約を追加。 |
| FIND-DRE-016 | 品質ゲート・設定共通化 | Medium | `doctor-backend-oracle.ps1`がプロセス環境変数だけを確認し、共通runnerのローカル設定読み込みと契約がずれていた。 | 対応済み | `oracle-test-helpers.ps1`を共通利用し、`-ConfigPath`と既定ローカル設定に対応。root Doctorにも`DAILY_REPORT_ALLOW_DDL`を追加。 |
| FIND-DRE-017 | 品質ゲート・Coverage | Medium | Backend CoverageのMaven終了コードは0だったが、JaCoCoが`jacoco.exec`不足でreport/checkをスキップし、既存レポートの存在確認だけが成功した。 | 対応済み | JaCoCo `prepare-agent`の`destFile`引数を確認し、日本語を含むWindowsパスでagent引数が文字化けする原因を特定。`backend/scripts/test-oracle.ps1`へ一時ASCIIドライブマッピングと終了時解除を追加し、BackendCoverageで実行データ・report・checkを再生成。 |

## 2026-07-19 テスト構成・責務分割再発防止

| 指摘ID | 分類 | 重要度 | 指摘内容 | 対応状況 | 対応内容・記録先 |
| --- | --- | --- | --- | --- | --- |
| DR-PROC-004 | プロセス・テスト構成 | High | 全テストが成功していても、認証・一覧・登録編集などの独立責務が一つのテストファイルへ集中していないかを、実装前計画とFull実行前のレビュー観点として明示していなかった。今回、`App.test.tsx`の責務混在を後から認識して分割したため、テストケースの網羅性とテスト構成の保守性を同時に確認する手順が不足していた。 | 対応済み | `実装前チェック表.md`、`実装後レビュー表.md`、`テスト方針.md`、`テストケースレビュー観点.md`、`開発フロー.md`、`AI専門レビュー用プロンプト定義.md`、`projectfoundation-preflight-ja`、`projectfoundation-review-ja`へ「テスト構成・責務分割」をP0観点として追加。実装前はテスト構成・責務対応表を作成し、実装後はFull前に分割・維持判断を記録する。`日報登録編集_実装後レビュー_2026-07-18.md`、`AGENTS.md`。 |

## 2026-07-19 日報承認・差戻し Task 1レビュー修正

| 指摘ID | 分類・重要度 | 問題 | 対応 | 標準化判定 | 再確認・保留 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-REV-001 | DB schema / Important | fresh schemaの`schema-login.sql`だけが承認監査3列を欠き、Entity/fixtureと不整合だった。 | `schema-daily-report.sql`と同じ3列を追加。既存schema向け冪等migrationは維持し、DBを直接変更しない。 | 既存観点で対応。DDL正本・DB整合性レビューで検出可能。 | 静的DDL一致は成功。専用TEST schemaをresetできる安全なrunnerでfresh schema作成を再確認。 |
| FIND-APR-REV-002 | fixture再現性 / Important | 担当外`U099`が残存データに依存し、上長名期待値がtest profileの正本と違った。 | `U099`を明示seedし、各テスト前後にcleanup、期待値を`佐藤 花子`へ統一。 | 既存観点で対応。test support/fixture再現性レビューで検出可能。 | Oracle focused test成功。テスト順序・残存データなしの実行を継続確認。 |
| FIND-APR-REV-003 | API/DBテスト網羅 / Important | TC/RTが求める詳細監査、1000文字境界、全非PENDING、全ロール、未認証/CSRF、DB不変の観測が不足した。 | `RT-APR-BE-001`～`005`、`009`、`010`のテスト名・アサーションを追加/更新し、正本ケースの実テスト名も更新。 | 既存観点で対応。状態・境界・認証/CSRF・副作用・トレーサビリティ観点で検出可能。 | Oracle focused 29件成功。UI/E2E/coverageは今回未実行で、該当品質ゲートで再確認。 |
| FIND-APR-REV-004 | 観測可能性 / Important | 承認/差戻しのcontroller/method mapとSecurity前path resolverがなく、ログのfeature/useCaseが`UNKNOWN`だった。 | MVC mapとpath resolverへ`DAILY_REPORT`/`APPROVE`・`REJECT`を追加し、focused testを追加。機微な本文・氏名・tokenはログに追加しない。 | 既存観点で対応。ログ相関・機密情報レビューで検出可能。 | 修正前REDと修正後focused test成功を確認。Oracleログで401/403/業務エラーの分類を確認。 |

## 2026-07-19 日報承認・差戻し Task 2レビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-T2-REV-001 | 未承認一覧APIのテスト網羅・期待結果 / Important | 未承認一覧のfilter、複数件順序、許可なし/担当外group空配列、上長の日付境界、未認証401優先を自動テストで観測できなかった。 | 対応済み。RT-APR-BE-006へfilter・PENDING/担当外除外・順序・空配列・400を、RT-APR-BE-007へ未認証401と非上長403を追加。 | **既存観点で対応**。既存の認証、境界、組合せ、期待結果、Controller API/DB責務で検出可能。 | `DailyReportApprovalControllerTest`、TC-APR-008/009、`日報承認差戻し_作業記録.md`。保護Oracle wrapperでApproval 13件のfailures/errors 0を確認。 |
| FIND-T2-REV-002 | 詳細API監査情報と認可の回帰テスト不足 / Important | 監査項目が存在する承認済み・差戻し済み日報について、3許可ロール、担当外上長403の無漏えい、不存在404を一つの回帰契約として確認していなかった。 | 対応済み。RT-APR-BE-008でAPI経由の遷移後に3許可ロールの監査JSON、担当外上長403と監査フィールド不存在、不存在404を追加。 | **既存観点で対応**。既存セキュリティ規約の所有者/ロール/IDORと状態遷移後副作用で検出可能。 | `DailyReportApprovalControllerTest`、TC-APR-010、`日報承認差戻し_作業記録.md`。保護Oracle wrapperでApproval 13件のfailures/errors 0を確認。 |

## 2026-07-19 日報承認・差戻し Task 2再レビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-T2-REREV-001 | 詳細API認可・監査情報非露出 / Important | 担当外上長の詳細API 403テストで、監査ID・日時・コメントは非露出確認していたが、`approverName` と `rejectorName` の非露出を確認していなかった。 | 対応済み。`assertAuditIsNotLeakedToOutsideManager` に両氏名フィールドの `doesNotExist()` を追加し、HTTP 403と共通エラーコード `FORBIDDEN`、既存の監査フィールド非露出アサーションを維持した。 | **既存観点で対応**。既存セキュリティ規約のIDOR/エラー非露出と、テストケースレビュー観点の権限・所有者/期待結果で検出可能。 | `DailyReportApprovalControllerTest`、TC-APR-010、`日報承認差戻し_作業記録.md`、`.superpowers/sdd/task-2-fix-report.md`。指定Oracle wrapperでApproval 13件、failures/errors/skipped 0を再確認。承認済み・差戻し済み双方の403経路を継続確認する。 |

## 2026-07-20 日報承認・差戻し Task 3レビュー指摘修正

| 指摘ID | 分類・重要度 | 問題 | 対応状況・対応内容 | 標準化判定 | 再確認・保留 |
| --- | --- | --- | --- | --- | --- |
| FIND-T3-REV-001 | DTO契約・型安全性 / Important | `DailyReportResponse` の承認者ID・氏名・日時と差戻し者IDがoptionalになり、Backend DTOが返すnullキーを欠落可能な型へ緩めていた。一覧の既存 `approverName`/`approvedAt` 契約も後退していた。 | 対応済み。`frontend/src/dailyReport/types.ts` で詳細の監査4項目を必須 `string-or-null` に戻し、`DailyReportListItem` の `approverId`/`approverName`/`approvedAt` も必須 `string-or-null` として明示。`frontend/test/dailyReportApi.test.ts` と `frontend/test/support/dailyReportTestSupport.tsx` にnull値fixture/typecheck確認を追加・補完した。 | **既存観点で対応**。null・未指定、Backend DTO照合、型安全性、回帰テストの既存観点で検出可能。標準化資料の追加は不要。 | Focused 2 files/14 tests、Full 13 files/98 tests、typecheck、lint、build、test-layoutが成功。Frontend外のE2E/coverage/Backend/Oracle/fresh schemaは未実行。 |
| FIND-T3-REV-002 | APIエラー伝播テスト不足 / Important | 新規 `approveDailyReport`、`rejectDailyReport`、`fetchPendingApprovals` が共通 `ApiError` をそのままrejectすることをwrapperごとに直接確認していなかった。 | 対応済み。`frontend/test/dailyReportApi.test.ts` に承認409/`INVALID_STATUS`、差戻し400/`VALIDATION_ERROR`、未承認一覧403/`FORBIDDEN`を追加し、`code`、`message`、`details`、`requestId`の完全なreject値をアサート。成功/URL/CSRFテストと既存APIエラー回帰は維持した。 | **既存観点で対応**。異常系、エラー処理、API client共通化、期待結果の既存観点で検出可能。標準化資料の追加は不要。 | Focused 2 files/14 tests、Full 13 files/98 tests、typecheck、lint、build、test-layoutが成功。未実行項目はFIND-T3-REV-001と同じ。 |

## 2026-07-20 日報承認・差戻し Task 4 UIレビュー指摘修正

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 再確認・保留 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-UI-001 | 検索UI・受入条件 / Important | 上長の未承認一覧に日付範囲、グループ、社員、検索、条件クリアがなく、既存criteria/APIを画面から使えなかった。 | 対応済み。`DailyReportPendingApprovalList.tsx`へ当月初期・clear、入力検証、選択条件での取得、loading/error/emptyを追加。`RT-APR-UI-001`で初期/選択/エンコード/clear、検証、空/エラーを確認した。 | **既存観点で対応**。検索条件、入力検証、期待結果、異常系の既存観点で検出可能。 | 3 focused files 41 tests、Full 14 files 124 tests、typecheck/lint/build/test-layout成功。Mock E2E/coverage/支援技術実機は後続ゲートで再確認。 |
| FIND-APR-UI-002 | 画面遷移・状態制御 / Important | 詳細から社員の`DRAFT`/`REJECTED`編集、および一覧復帰へ進めず、`/edit`互換性のUI証跡も不足した。 | 対応済み。社員の許可状態だけへエンコード済み編集リンク、全許可詳細へロール別一覧リンクを追加。上長・管理者・`PENDING`・`APPROVED`は編集リンクなし。`RT-APR-UI-004`とCalendar/App回帰で詳細/`/edit`を確認した。 | **既存観点で対応**。状態別操作、経路回帰、認可UIの既存観点で検出可能。 | 同上。差戻しから再提出までの実ブラウザE2Eは`approval-rejection.spec.ts`で後続確認。 |
| FIND-APR-UI-003 | アクセシビリティ・操作安全性 / Important | 差戻しモーダルの初期フォーカス、Tab循環、Escape、起動元復帰、背景操作停止が未実装だった。 | 対応済み。textarea focus、Tab/Shift+Tab trap、Escape/cancel、trigger focus restore、背景`inert`/`aria-hidden`と操作disabledを追加。`RT-APR-UI-004`でキーボード経路と背景承認disabledを確認した。 | **既存観点で対応**。実装後レビュー表のアクセシビリティ/安全側UI観点で検出可能。 | Unit/jsdomで確認。実ブラウザ・支援技術による操作確認は後続アクセシビリティゲートで再確認。 |
| FIND-APR-UI-004 | テスト不足・トレーサビリティ / Important | 条件、clear、空/エラー、経路互換、状態別制御、mutation異常/読込中、ダイアログkeyboardのUI証跡とRT対応が不足した。 | 対応済み。`RT-APR-UI-001`～`004`をテスト名に明示し、承認専用、Calendar既存回帰、App経路を責務別に追加/維持。401/403/409/generic、loading disablementを確認した。 | **既存観点で対応**。正本ケース、トレーサビリティ、異常系、テスト構成・責務分割の既存観点で検出可能。 | 同上。E2E/browser/coverageは未実行の後続ゲートとして成功扱いにしない。 |

## 2026-07-20 日報承認・差戻し Task 4 最終再レビュー

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 再確認・保留 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-UI-REREV-001 | トレーサビリティ / Minor | UIテスト名には `RT-APR-UI-*` があるが、`TC-APR-*` ラベルが併記されていない。 | 対応済み（非ブロッキング）。正本テストケースと作業記録の対応表で `TC-APR-003/006/008/009/010` と `RT-APR-UI-001～004` の対応を保持し、既存のテスト責務分離・命名を変更しない。 | **既存観点で対応**。テストケースレビューのトレーサビリティ観点で検出可能。 | Task 6の最終トレーサビリティ照合で一意対応を再確認する。 |

## 2026-07-20 日報承認・差戻し Task 5 Mock E2Eレビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 再確認・保留 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-T5-001 | Canonical E2E業務フロー / Important | 差戻しが上長画面だけで終わり、社員の最新コメント確認、編集、更新、再提出、`PENDING`復帰を確認していなかった。 | 対応済み。`approval-rejection.spec.ts`のTC-APR-007/RT-APR-E2E-002で別Browser Contextの社員へ状態を引き継ぎ、詳細確認、`編集する`、`PUT`、`/resubmit`、`PENDING`、pending詳細の編集リンク非表示を確認。`dailyReportApprovalMocks.ts`は日報状態だけを共有し、認証状態を共有しない。 | **既存観点で対応**。状態遷移完結性・canonical case/実テスト対応で検出可能。 | 対象Mock E2Eは3/3成功。既存`daily-report.spec.ts`は今回中断で未判定のため、Task 6で完走・判定する。 |
| FIND-APR-T5-002 | 認証/current-user・監査fixture契約 / Important | 上長・管理者が社員fixtureを継承し、監査ID/氏名もBackend正本と異なっていた。 | 対応済み。`authMocks.ts`をDataInitializer正本のcurrent-user契約へ合わせ、管理者/上長に架空の社員ID・所属/勤務設定を持たせない。承認・差戻し監査は`U002/佐藤 花子`とし、ログインと監査レスポンスで確認。 | **既存観点で対応**。認証/認可、fixture再現性、監査期待結果で検出可能。 | 対象Mock E2E 3/3、unit 124/124、typecheck/lint/build/layout成功。Backend/Oracle/real-backend E2EはTask 6で再確認。 |
| FIND-APR-T5-003 | トレーサビリティ / Minor | 個別E2Eテスト名にRTだけがあり、TCを直接検索できなかった。 | 対応済み。3テスト名へ対応する`TC-APR-*`と`RT-APR-E2E-*`を併記。 | **既存観点で対応**。テストケースレビューのトレーサビリティ観点で検出可能。 | Task 6の最終照合でTC/RTの一意対応を再確認。 |

## 2026-07-20 日報承認・差戻し Task 5独立再レビュー

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 再確認・保留 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-T5-REREV-001 | 認証/current-user fixture契約 / Important | 社員のCurrentUser fixtureにBackend APIが返さない`employeeId`が混入し、認証レスポンス契約と日報seed識別子が混在していた。 | 対応済み。`authMocks.ts`から`employeeId`を削除し、`employeeReportIdentity`へ日報seed専用の`E001/山田 太郎`を分離。社員・上長・管理者のログインpayloadを全キー一致で検証し、余分なキーを検出する。 | **既存観点で対応**。認証payload忠実度、fixture再現性、期待結果の既存観点で検出可能。 | 承認Mock E2E 3/3、E2E typecheck成功。Backend/OracleのCurrentUser実データ照合はTask 6で継続。 |
| FIND-APR-T5-REREV-002 | E2E TypeScript/static gate / Important | E2E支援コードが通常のfrontend typecheck対象外で、状態遷移のnullable型エラーを実行時まで検出できなかった。 | 対応済み。モック状態を`DailyReportResponse`で型付けし、`@types/node`、`frontend/tsconfig.e2e.json`、`typecheck:e2e` scriptを追加。全E2E TypeScriptを専用ゲートで検証する。 | **標準化候補**。今回の専用ゲートをローカル品質ゲートへ追加した。CI/workflowへの組込要否はTask 6で確認する。 | `npm.cmd --prefix frontend run typecheck:e2e`成功。CIへの組込は品質ゲート運用資料とworkflowを再確認する。 |
| FIND-APR-T5-REREV-003 | Canonical期待結果・テスト不足 / Important | 承認後操作消失・未承認一覧除外、再提出後の提出日時・最新差戻し監査情報が未アサートだった。 | 対応済み。`approval-rejection.spec.ts`へ承認後ボタン非表示・一覧除外、再提出後`submittedAt`・差戻し者・コメントのアサーションを追加。 | **既存観点で対応**。canonicalケースの期待結果・状態遷移・テスト検出力の既存観点で検出可能。 | 承認Mock E2E 3/3、既存日報E2E 13/13、Unit 124/124、通常/E2E typecheck、lint/build/layout成功。 |

## 2026-07-20 日報承認・差戻し Task 6最終レビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-FINAL-001 | UI安全性・Important | 承認ボタンが直接APIを呼び、設計の確認操作、二重送信抑止、背景操作停止を満たしていなかった。 | 対応済み。確認ダイアログを追加し、キャンセル/EscapeではAPIを呼ばず、確定時だけ1回呼ぶ。loading中の再送を抑止し、背景を`inert`/`aria-hidden`にした。 | **既存観点で対応**。UI安全側、アクセシビリティ、状態遷移の既存観点で検出可能。 | `DailyReportDetail.tsx`、UI test、`approval-rejection.spec.ts`。Approval E2E 3/3、正式E2E 16/16。 |
| FIND-APR-FINAL-002 | E2E品質ゲート・Important | `typecheck:e2e`がFormal E2E gateへ接続されず、CIでE2E fixtureの型ずれをblockingできなかった。 | 対応済み。`e2e:run`、`check.ps1 -CiTask E2E`の先行typecheck、GitHub Actions名/コメント、品質ゲート運用資料を更新。 | **標準化候補**。E2E TypeScriptのblocking検査として他機能へ横展開可能。 | `frontend/package.json`、`scripts/check.ps1`、`.github/workflows/quality.yml`、品質ゲート運用。正式E2E typecheck + Playwright 16/16。 |
| FIND-APR-FINAL-003 | DB不変・Important | invalid-stateテストが全監査列nullのため、非null監査値が不変であることを証明していなかった。またapprove/reject不存在404が不足していた。 | 対応済み。APPROVED/REJECTEDの固定監査fixture、更新前後の全行比較、approve/reject各404を追加。 | **既存観点で対応**。状態、DB整合性、副作用なし、不存在境界で検出可能。 | `DailyReportTestSupport.java`、`DailyReportApprovalControllerTest.java`。Oracle focused 15件成功。 |
| FIND-APR-FINAL-004 | 実装後レビュー記録・Important | Task 6のセキュリティ/可読性/実装後レビュー、受入条件・テストケース・指摘一覧・作業記録の最終同期が不足していた。 | 対応済み。専門レビュー3文書を追加し、受入条件レビュー、テストケースレビュー、作業記録、本指摘一覧へ最終結果・未実行条件を追記。 | **既存観点で対応**。実装後レビュー運用・トレーサビリティで検出可能。 | `日報承認差戻し_セキュリティレビュー.md`、`可読性レビュー.md`、`実装後レビュー.md`ほか。最終レビューで再確認。 |
| FIND-APR-FINAL-005 | 共通化・Minor | 承認Serviceが担当グループ判定を共通Policyと重複実装していた。 | 対応済み。`DailyReportAccessPolicy.requireManager`を承認Serviceから利用し、重複Role判定を削除。 | **既存観点で対応**。共通部品化判断基準・認可レビューで検出可能。 | `DailyReportApprovalService.java`。Checkstyle/SpotBugs、Full成功。 |
| FIND-APR-FINAL-006 | トレーサビリティ・Minor | Backend/UIテスト名へTC IDが直接付いておらず、TC/RTの追跡に推測が必要だった。 | 対応済み。Backendコメント、UIテスト名、E2Eテスト名へTC/RTを付与し、責務別対応を再照合。 | **既存観点で対応**。テストケースレビューのトレーサビリティ観点で検出可能。 | `DailyReportApprovalControllerTest.java`、`DailyReportApprovalPanel.test.tsx`、`approval-rejection.spec.ts`。最終レビューで一意対応を確認。 |

## 2026-07-20 日報承認・差戻し 最終ゲート記録

- Full成功、正式E2E成功（E2E TypeScript typecheck + Playwright 16/16）、FrontendCoverage成功（125 tests、Statements 96.36%、Branches 94.58%、Functions 97.23%、Lines 96.22%）。
- Oracle focusedはApproval 15、Search 17、Submission 5、計37件でFailures/Errors/Skipped 0。BackendCoverageはOracleテスト107件自体は成功したがJaCoCo実行データ/レポートが生成されず保留。fresh schema resetとreal Backend E2Eも未実行。
- Quickのstaged-secretsはローカルに`gitleaks`がないため失敗。差分へ資格情報・保護Oracle設定を含めていないことを確認し、導入後に再実行する。

## 2026-07-21 PR #7 CIレビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-APR-PR-001 | CI互換性・Important | `Full / Linux`のOracle preflight契約テストがWindows専用`cmd.exe`を無条件に呼び、Linuxで`cmd.exe is not recognized`となった。 | 対応済み。`oracle-preflight.tests.ps1`でWindowsは`.cmd`、非Windowsは`test-oracle.ps1`を直接実行する分岐へ修正。修正前RED、修正後Windows契約テストGREENを確認。 | **標準化候補**。CI契約テストのOS依存シェル呼出しをクロスプラットフォーム観点として横展開する。 | PR #7の`Full / Linux`を再実行し、6件の必須チェックが成功すること。 |

## 2026-07-22 日報詳細UI調整レビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-DUI-001 | トレーサビリティ・記録 / Important | 仕様に定義したAC-DUI/TC-DUI/RT-DUIが、正本受入条件・正本テストケース・受入条件レビュー・作業記録・指摘一覧へ未同期だった。 | 対応済み。F-005-AC-010～012、TC-DUI-001～004、RT-DUI-001～004を各正本・記録へ追加し、既存RT-APR-UI-004との回帰関係を明記した。 | **既存観点で対応**。既存のトレーサビリティ・記録レビューで検出可能。 | `機能一覧・受入条件.md`、`日報承認差戻し_テストケース.md`、`日報承認差戻し_受入条件レビュー.md`、`日報詳細UI調整_実装後レビュー_2026-07-22.md`、作業記録。再確認条件なし。 |
| FIND-DUI-002 | UI表示テスト・期待結果 / Important | jsdomのDOM確認だけでは、操作余白、PENDINGピルの背景色、全周ボーダーのCSS適用結果を確認できなかった。 | 対応済み。承認詳細E2EへRT-DUI-003を追加し、computed gap、背景色、上枠色を実ブラウザで確認した。 | **標準化候補**。CSS適用結果を受入条件とする画面では、DOM単体テストに加えてE2E/目視を要求する観点を標準資料へ反映した。 | `テストケースレビュー観点.md`「UIスタイル観測」、`テスト方針.md`。今後のCSS表示受入条件レビューで横展開する。 |
| FIND-DUI-003 | テスト検出力・回帰 / Important | TC-DUI-004単体では既存承認・差戻しAPI呼出しと再取得の不変性を直接証明していなかった。 | 対応済み。正本ケースと受入条件レビューで既存RT-APR-UI-004、承認関連E2E、FullテストをAC-DUI-004へ紐付け、重複テストを作らず回帰を確認した。 | **既存観点で対応**。既存の回帰・期待結果・トレーサビリティ観点で検出可能。 | `日報承認差戻し_テストケース.md`、`日報詳細UI調整_実装後レビュー_2026-07-22.md`、作業記録。再確認条件なし。 |

## 2026-07-25 DB基本設計書追加レビュー指摘

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-DBDOC-001 | DB定義・Oracle型 / Minor | `TIMESTAMP WITH LOCAL TIME ZONE` の保持・表示説明がOracle仕様より粗かった。 | 対応済み。DBタイムゾーンに正規化して保持し、セッションのタイムゾーンで表示する説明へ修正した。 | **既存観点で対応**。外部公式資料確認とDB型レビューで検出可能。 | `基本設計書/DB定義.md`、`DB基本設計書追加_2026-07-25.md`。再確認条件なし。 |
| FIND-DBDOC-002 | DB設計・差異 / Medium | 概念設計では`groups`を想定しているが、現行物理DBには独立テーブルがなく、グループ情報を利用者・日報へ保持している。 | 保留。現行DBを正本として差異と現行の責務をDB定義書へ記載した。 | **保留**。グループマスタ管理機能の追加時に正規化要否を再確認する。 | `基本設計書/DB定義.md`「概念設計・現行DDLとの差異と保留事項」。 |
| FIND-DBDOC-003 | DB制約 / Medium | `break_type_periods`に現行DDL上の主キー・一意制約がない。 | 保留。`break_type_id`と`display_order`を論理識別単位として記録した。 | **保留**。マスタメンテナンス導入時または重複登録防止が要件化された時点で再確認する。 | `基本設計書/DB定義.md`「break_type_periods」「差異と保留事項」。 |
| FIND-DBDOC-004 | DB監査整合性 / Medium | `approver_user_id`と`rejector_user_id`に現行DDL上の外部キーがない。 | 保留。ロール・権限のアプリケーション検証責務とDB側整合性の保留条件を記録した。 | **保留**。DB側の監査参照整合性が要件化された時点で再確認する。 | `基本設計書/DB定義.md`「daily_reports」「差異と保留事項」。 |

## 2026-07-25 日報画面の時刻表示確認

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-TIME-UI-001 | UI表示・Medium | 提出日時・承認日時・差戻し日時を、`DailyReportDetail.tsx`、`DailyReportCalendarList.tsx`、`DailyReportPendingApprovalList.tsx`、`DailyReportForm.tsx`でAPIのISO 8601文字列（例: `2026-07-17T09:00:00+09:00`）のまま画面へ描画している。画面表示用の日時フォーマット処理がなく、利用者向けの日時表示としては未整形である。一方、勤務開始・終了時刻と勤務時間はBackendの`TimeRules.formatTime`/`formatDuration`または`H:mm`変換で整形されている。 | 対応済み。`frontend/src/dailyReport/dateTimeFormat.ts`に共通フォーマッタを追加し、4画面で`YYYY-MM-DD HH:mm:ss`表示へ統一した。null/空文字は`-`、ミリ秒は非表示、元のオフセット時刻は変換しない。フォーマッタUnitと画面回帰テストを追加・更新した。 | **既存観点で対応**。形式チェック、画面の期待結果、タイムゾーン・日付境界の既存観点で検出可能。共通日時フォーマッタを追加する場合は、日報画面以外への適用範囲とタイムゾーン方針を設計してから対応する。 | `frontend/src/dailyReport/dateTimeFormat.ts`、`DailyReportDetail.tsx`、`DailyReportCalendarList.tsx`、`DailyReportPendingApprovalList.tsx`、`DailyReportForm.tsx`、`frontend/test/dailyReportDateTimeFormat.test.ts`、対象画面テスト。Focused 58件、Full 132件、typecheck/lint/build成功。 |

## 2026-07-25 品質ハーネス確認

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-TIME-UI-002 | UI表示テスト・Medium | 日報日時の表示仕様を変更した後も、Playwrightの4箇所が旧ISO 8601文字列を期待しており、ブラウザ回帰確認が失敗する。対象は承認日時、差戻し日時、再提出後の提出日時、編集画面の差戻し日時である。 | 対応済み。`approval-rejection.spec.ts`の3箇所と`daily-report.spec.ts`の1箇所を`YYYY-MM-DD HH:mm:ss`へ更新し、対象3テストを実行して3/3成功した。 | **既存観点で対応**。画面仕様変更時のE2E期待結果・回帰テスト同期の既存観点で検出可能。 | `frontend/e2e/approval-rejection.spec.ts:56,114,158`、`frontend/e2e/daily-report.spec.ts:302`。Simple BrowserCase、E2E型チェック、Full品質ゲートで再確認済み。 |
| FIND-QH-001 | 品質ハーネス・Medium | Simple品質ハーネスのBrowserCaseが、リポジトリルートから`npm.cmd --prefix frontend exec -- playwright test`を実行すると、`frontend/test`のVitestファイルをPlaywright対象として読み込み、ブラウザケース本体の確認前に停止する。 | 対応済み。BrowserCase定義にFrontend作業ディレクトリを設定し、`scripts/simple-mode.tests.ps1`へ境界契約テストを追加した。契約テスト、Simple BrowserCaseとも成功した。 | **標準化候補**。表示要件を含むSimple Mode全体へ再発するため、品質ゲート運用へFrontend作業ディレクトリ基準と契約テストを追記した。 | `scripts/check.ps1`の`New-SimpleBrowserCheckDefinition`、`scripts/simple-mode.tests.ps1`、`品質ゲート運用.md`。再確認済み。 |

## 2026-07-25 品質ゲート未実行の成功扱い再発防止

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-QH-002 | 品質ハーネス・High | 表示要件のBrowserCaseが起動できない場合に`BrowserManualReason`を指定すると、manual fallbackが終了コード0となり、`All requested checks passed.`が表示されていた。ブラウザ未実行を表示要件の品質ゲート成功と誤認できる。 | 対応済み。`scripts/check.ps1`のmanual fallbackを`BROWSER_UNVERIFIED`例外へ変更し、`simple-mode.tests.ps1`で失敗名・非0終了・成功サマリ非出力を契約化する。 | **標準化候補**。実行済み、未実行、CI委譲の判定は表示要件を持つ全Simple変更に共通するため、Review Skillと品質ゲート運用へ反映した。 | `scripts/check.ps1`、`scripts/simple-mode.tests.ps1`、`docs/AI活用開発研究/構想メモ/標準化/skills/projectfoundation-review-ja/SKILL.md`、`.agents/skills/projectfoundation-review-ja/SKILL.md`、`品質ゲート運用.md`。契約テストとSimple manual fallback再確認済み。 |

## 2026-07-28 F-002 Backend完成ケース追加

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| DR-T-001 | テスト不足・Medium | AM_OFF、PM_OFF、複数明細、休日作業ありの登録成功ケースが未実行だった。 | `TC-F002-BE-018`、`TC-F002-BE-020`～`TC-F002-BE-022`を正本ケースへ追加し、同一APIテスト責務で実装する。 | **既存観点で対応**。休日区分別入力、複数明細、合計一致の既存観点で横展開可能。 | `日報登録編集_テストケース.md`、F-002表示データ。BackendUnit/Oracle実行後に成功結果と証跡を同期する。 |
| DR-T-002 | 境界値テスト不足・Medium | 作業時間0分の拒否はあるが、1分登録成功の境界値が未実行だった。 | `TC-F002-BE-019`を正本ケースへ追加し、1分明細と登録後の合計を確認する。 | **既存観点で対応**。作業時間の下限境界値として他機能にも適用可能。 | `日報登録編集_テストケース.md`、F-002表示データ。BackendUnit実行後に成功結果を記録する。 |
| DR-T-003 | 認可テスト不足・Medium | 別社員seedを用いた社員本人の登録所有者確認が不足していた。 | `TC-F002-BE-023`、`TC-F002-BE-024`を追加し、認証主体のスナップショット固定とクライアント指定値の無効化を確認する。 | **既存観点で対応**。認証主体・所有者固定の認可境界として横展開可能。 | `日報登録編集_テストケース.md`、F-002表示データ、Backendテスト。複数社員seedとOracle実行後に再確認する。 |
| DR-T-004 | API期待結果テスト不足・Medium | TimeRulesで残業・深夜計算を確認していたが、登録後GETレスポンスの分数値・表示値を明示検証していなかった。 | `TC-F002-BE-030`、`RT-F002-BE-023`を追加し、Oracleで登録後GETレスポンスを検証する。 | **既存観点で対応**。APIの期待結果・業務計算結果の明示アサーションとして横展開可能。 | `日報登録編集_テストケース.md`、F-002表示データ、`DailyReportCommandControllerTest.java`。Oracle 25件実行後に再確認する。 |

## 2026-07-28 F-002 Backend完成後の指摘再判定

| 指摘ID | 対応状況 | 対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- |
| DR-T-001 | 対応済み | 複数明細、休日作業、AM_OFF、PM_OFFを`DailyReportCommandControllerTest`へ追加し、Oracle focused 24件で成功。 | **既存観点で対応**。休日区分別入力、合計一致、複数明細の観点で検出可能。 | 正本テストケース、F-002表示データ、受入条件レビュー、作業記録。再確認条件なし。 |
| DR-T-002 | 対応済み | 1分登録を追加し、Oracle focused 24件で実行成功。 | **既存観点で対応**。作業時間下限境界値として横展開可能だが、既存の境界値観点で検出できる。 | 正本テストケース、F-002表示データ、作業記録。再確認条件なし。 |
| DR-T-003 | 対応済み | `employee002`をseedへ追加し、社員スナップショットとクライアント側所有者変更無効化をOracle focused 24件で確認。 | **既存観点で対応**。認証主体・所有者固定の認可境界として検出可能。 | `DataInitializer.java`、`DailyReportCommandControllerTest.java`、正本記録。再確認条件なし。 |
| DR-T-004 | 対応済み | `createWorkdayReportReturnsOvertimeAndNightMinutes`を追加し、Oracle focused 25件で残業255分、深夜60分、表示値4:15/1:00を確認。 | **既存観点で対応**。APIの期待結果・業務計算結果の明示アサーションとして横展開可能。 | `DailyReportCommandControllerTest.java`、`日報登録編集_テストケース.md`、F-002表示データ。再確認条件なし。 |
| FIND-DRE-017 | 対応済み | JaCoCo agentの日本語パス文字化けを一時ASCIIドライブマッピングで回避し、BackendCoverageを132件成功、JaCoCo全指標85%以上で再確認。 | **標準化候補**。Windows非ASCIIパスでのagent引数互換性は全Backend Coverageに再発し得るため、品質ゲート・Coverage runnerの契約テストへ反映対象。 | `backend/scripts/test-oracle.ps1`、作業記録。カバレッジ実行時に`jacoco.exec`とXMLを確認する。 |

## 2026-08-02 Oracle preflight契約テスト修正

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-QH-003 | 品質ハーネス・Medium | `oracle-preflight.tests.ps1`がworkflow全体の`continue-on-error: true`を一律に禁止し、Oracle集約ジョブのartifact取得用設定をテスト失敗の成功扱いとして誤検出していた。また、GitHub Actionsの`if: ${{ always() }}`を`if: always()`としてのみ検査していた。 | 対応済み。workflowをstep単位で検査し、`continue-on-error: true`と`run:`を同一stepに持つテスト・coverage実行だけを違反とした。`always()`の式記法も正しく契約化した。Oracle workflow本体は変更していない。`oracle-preflight.tests.ps1`および`FullBackend`で成功を確認した。 | **既存観点で対応**。workflowの失敗伝播・失敗時診断は既存の品質ゲート契約で検出でき、今回の修正は契約テストのstep境界誤認の是正である。 | `scripts/oracle-preflight.tests.ps1`、`PMD複雑度ルール適用_2026-08-02.md`。再確認条件なし。 |

## 2026-08-02 品質ゲート実行時間調整

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-QH-004 | 品質ハーネス・実行時間 / Medium | テスト実行時の初期timeoutが3分では、依存取得・初回キャッシュ構築・FullBackend/Fullの実行で上限に到達し、timeoutを変更して再実行する手戻りが発生する。 | 対応済み。Quality workflowの全8 jobへ`timeout-minutes: 10`を設定し、Oracle/Securityの30分は維持した。ローカル初期timeoutをQuick/PrePush 3分、FullBackend/Full 10分、Oracle系30分として運用資料へ追加し、workflow契約テストで固定した。 | **標準化候補**。品質ゲート全体に再発する実行環境・キャッシュ依存のため、job timeoutとローカル初期値を品質ゲート運用の共通基準とする。 | `.github/workflows/quality.yml`、`scripts/impact-workflow.tests.ps1`、`品質ゲート運用.md`、`品質ゲート実行時間調整_2026-08-02.md`。FullBackendとpre-pushで再確認する。 |

### 未解消・保留

- `DR-S-004`: 本番相当CookieのSecure / SameSite属性は環境確認が必要。分類は`保留`、再確認条件は本番相当設定での画面セッション確認。
- `QG-F002-SCREEN`: Mock E2E成功済みだが、実ブラウザ画面確認は未完了。

## 2026-08-03 新レビュー基準によるF-002日報登録画面レビュー

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-F002-REV-001 | 仕様整合・P0 | S-003/F-002が要求する自動算出休憩時間、実勤務時間、通常勤務時間、残業時間、深夜時間を登録画面で確認できない。Backendの詳細DTOには値があるが、`DailyReportForm`は休憩区分・勤務区分名だけを表示し、計算結果を表示していない。 | **修正済み**。保存後に詳細APIを再取得し、計算結果と作業時間合計を表示する。入力変更時は古い計算結果を消去する。 | **個別対応**。S-003の画面/API契約としてADR-DRE-001へ記録した。 | `frontend/src/dailyReport/DailyReportForm.tsx`、`frontend/test/dailyReportForm.test.tsx`、`frontend/e2e/daily-report.spec.ts`、F-002-AC-002/003。Focused Unitと登録画面E2Eで再確認済み。 |
| FIND-F002-REV-002 | 入力・日付境界・P1 | 新規日報の初期日付が`toISOString()`のUTC日付になる。日本時間の2026-07-17 00:30は`2026-07-16`として初期化され、登録対象日を誤る可能性がある。 | **修正済み**。UTC変換をやめ、実行環境のローカル業務日付を生成する。日付境界Unitを追加した。 | **標準化候補**。共通観点へ反映済み。 | `frontend/src/dailyReport/DailyReportForm.tsx`、`frontend/test/dailyReportForm.test.tsx`、V-COM-001、F-002-AC-001。RT-F002-UI-002成功。 |
| FIND-F002-REV-003 | 業務ルール・保守性・P1 | 休日区分の入力可否をDBマスタの`requiresWorkTime`/`allowsWorkItems`ではなく、画面側の`PAID_LEAVE`/`HOLIDAY`固定分岐で判定している。マスタ変更や区分追加時にBackendと画面の制御が乖離する。 | **修正済み**。画面制御とFrontend入力検証を休日区分マスタ属性へ変更し、固定コード依存を除去した。 | **標準化候補**。マスタ属性駆動観点とADR-DRE-002へ反映済み。 | `frontend/src/dailyReport/DailyReportForm.tsx`、`frontend/src/dailyReport/dailyReportValidation.ts`、`frontend/src/dailyReport/types.ts`、F-002-AC-009/016。固定コード外区分Unit、通常／有給E2E成功。 |
| FIND-F002-REV-004 | 入力安全性・保守性・P1 | マスタ取得前の作業明細追加で`P001`/`WC001`を固定フォールバックする。初期データや有効状態が変わると、利用者に見えない値で保存要求を作り、登録時に失敗するか誤った前提を残す。 | **修正済み**。初期値・追加値を取得済みマスタから生成し、案件・作業分類が未取得または空の場合は追加を無効化した。 | **個別対応**。今回の画面のマスタ取得失敗状態としてADR-DRE-002へ記録した。 | `frontend/src/dailyReport/DailyReportForm.tsx`、`frontend/test/dailyReportForm.test.tsx`。マスタ空集合Unit成功。 |
| FIND-F002-REV-005 | 入力検証・P1 | JSONの`workItems`に`null`要素が含まれると、Bean Validationで要素のnullが検出されず、`TimeRules.validateItems`が要素を直接参照して想定外例外へ進む可能性がある。利用者向け400ではなく500になる入力境界が残っている。 | **修正済み**。DTOでnull要素をBean Validationの対象にし、正規化時に検証前のNPEを発生させない。 | **既存観点で対応**。入力境界・共通エラー契約の既存観点で対応した。 | `backend/src/main/java/com/example/dailyreport/report/dto/DailyReportRequest.java`、`backend/src/test/java/com/example/dailyreport/report/DailyReportCommandControllerTest.java`。`workItems:[null]`の400確認済み。 |
| FIND-F002-REV-006 | UI安全側・競合・P1 | 保存中を表す状態がなく、保存・保存して提出ボタンが通信中も有効である。二重クリックで登録、更新、提出が重複実行され、既知の`FIND-DRE-006`（状態競合の原子性）と同じ事故調査負荷を増やす。 | **修正済み**。保存中状態、ボタン無効化、Refによる二重送信防止を追加し、新規保存後の詳細取得が提出後状態を上書きしない順序へ変更した。 | **標準化候補**。保存中・通信順序観点として標準化資料へ反映済み。 | `frontend/src/dailyReport/DailyReportForm.tsx`、`frontend/test/dailyReportForm.test.tsx`、`frontend/e2e/daily-report.spec.ts`、`FIND-DRE-006`。Unit二重操作、登録提出E2E成功。 |
| FIND-F002-REV-007 | トレーサビリティ・保守性・P1 | 日報登録の重要判断（勤務結果のBackend計算、利用者スナップショット、明細全差し替え、マスタ駆動ルールと画面分岐の境界、登録レスポンスと詳細レスポンスの分離）を確認できるADRが存在せず、コードコメントと過去作業記録に分散している。 | **修正済み**。計算表示契約、マスタ駆動入力制御、履歴保持・明細更新方式をADR-DRE-001～003へ記録した。 | **標準化候補**。ADRテンプレート運用と標準化資料一覧へ反映済み。 | `docs/AI活用開発研究/設計判断記録/ADR-DRE-001-日報登録画面の計算表示契約.md`、`ADR-DRE-002-日報登録画面のマスタ駆動入力制御.md`、`ADR-DRE-003-日報登録更新の履歴保持と明細更新方式.md`。 |

## 2026-08-05 用語台帳反映後のコード・テストレビュー

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-F002-DOM-001 | 用語整合性・P2 | S-003の画面ラベルが`利用者`、`勤務開始`、`勤務終了`で、用語台帳・画面設計の正式用語である`社員名`、`勤務開始時刻`、`勤務終了時刻`と一致していない。また、Service/API clientのドメインコメントが`DRAFT`を主に「下書き」と表現している。 | **修正済み**。画面ラベルを`社員名`、`勤務開始時刻`、`勤務終了時刻`へ変更し、`DRAFT`の表示・設計書・メッセージを`下書き`へ統一した。認証・認可境界の「利用者」は意味を維持した。 | **既存観点で対応**。命名・用語台帳・画面期待結果の既存観点で対応した。 | `frontend/src/dailyReport/DailyReportForm.tsx`、`frontend/test/dailyReportForm.test.tsx`、`frontend/src/shared/messageCatalog.tsx`、`backend/src/main/java/com/example/dailyreport/master/MessageCatalogDefaults.java`、`backend/src/main/resources/db/oracle/seed-master-data.sql`、対象設計書。Frontend Unit 137件成功。 |
| FIND-F002-DOM-002 | 用語整合性・P2 | `workMinutes`が日報の実勤務時間と日報明細の作業時間の両方に使われ、Backendの計算結果、Entity、Response、Frontend型、テストヘルパーでも文脈が省略されている。API互換性上ただちに外部項目を変更する必要はないが、内部変数・テスト引数の誤読余地が残る。 | **修正済み**。外部API項目`workMinutes`は互換性のため維持し、内部の実勤務時間を`actualWorkMinutes`、日報明細の時間を`workItemMinutes`へ変更した。Frontend型にはAPI互換項目であることを注記した。 | **標準化候補**。異なる業務概念に同じ英語名を使う場合の所有オブジェクト明示を、関連機能へ横展開する。 | `TimeRules`、`DailyReportEntity`、`DailyReportWorkItemEntity`、DTO変換、`StoredReportRules`、`TimeRulesTest`、`DailyReportTestSupport`、`frontend/src/dailyReport/types.ts`。Backend Unit 54件、Oracle登録・提出・承認系47件成功。 |
| FIND-F002-DOM-003 | 状態競合・P2 | `V-TRN-010`は状態変更直前の再確認と同時操作時の不整合検出を要求するが、`DailyReportSubmissionService`と`DailyReportApprovalService`は読み込み後の状態判定に留まり、Entityに`@Version`または条件付き更新がない。テストも順次実行の不正状態確認で、同時実行を検出するケースがない。 | **修正済み**。社員本人の日報更新・提出・再提出と上長の承認・差戻しで対象行を`PESSIMISTIC_WRITE`取得し、先行処理のコミット後に後続処理が状態を再評価するようにした。 | **既存観点で対応**。状態遷移、排他・同時実行、DB不変性の既存観点で対応した。 | `DailyReportRepository`、`DailyReportApprovalService`、`DailyReportConcurrencyTest`、`TC-F002-BE-031`、`RT-F002-BE-024`。Oracleで後続提出APIの待機とコミット後`409 INVALID_STATUS`を確認。 |

## 2026-08-12 superpowers利用終了・Codex標準手順置換

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| DR-C-026 | ドキュメント運用・Medium | 現行のpreflight/review Skillが、任意の外部Skillであるsuperpowersの設計、計画、TDD、独立レビュー、レビュー授受、不具合調査機能を必須または推奨の実行入口として参照していた。superpowersを利用しない環境では品質重視モードの手順を完了できない。 | **対応済み**。Plan mode、必要時のGoal mode、Codexサブエージェント、`/review`、適用条件に合う`$tdd`・`$diagnosing-bugs`、ProjectFoundation正本手順へ置換し、編集元Skillと`.agents/skills`配布コピーを同期した。 | **標準化候補（反映済み）**。品質手順は任意の外部Skillを必須依存にせず、Codex標準機能、利用可能な汎用Skill、リポジトリ正本手順の責務を分ける。 | `開発フロー.md`の「Codex標準機能とSkillの利用」、preflight/review Skill、`superpowers利用終了_手順置換_2026-08-12.md`。現行実行名検索0件、Skill validator、コピー一致を再確認する。 |
| DR-C-027 | 実装計画・Medium | Git管理中の既存実装計画7件にsuperpowersの`executing-plans`または`subagent-driven-development`を要求する実行案内が残り、現在の手順で計画を再開できなかった。初回独立レビューでは、コード品質確認画面計画の末尾にも`executing-plans`が1件残っていた。 | **対応済み**。各計画の先頭を`/plan`での現状照合と必要時の`/goal`開始へ置換し、末尾の残存を「合意済みPlanに従って」へ修正した。独立再レビューはCLEAN。 | **既存観点で対応**。実装計画の実行入口は`開発フロー.md`のCodex標準機能運用に従い、再開前に現在状態を照合する。 | Git管理中の`docs/superpowers/plans` 6件、`コード品質確認画面_実装計画.md`、`superpowers利用終了_手順置換_2026-08-12.md`。過去の利用実績と旧パスは履歴として維持する。 |
| DR-C-028 | Skill運用・Medium | superpowers置換時に現在利用可能な汎用Skillを先に棚卸しせず、TDDと不具合調査を文章手順だけへ置換していた。`$tdd`と`$diagnosing-bugs`を利用できる構成が反映されていなかった。 | **対応済み**。Skill構成を組み込み、ユーザー共通、ProjectFoundation固有、Pluginへ分類し、`$tdd`、条件付き`$diagnosing-bugs`・`$codebase-design`、Skill文書変更時の`$writing-for-agents`・`$skill-creator`を開発フロー、Project Skill、`AGENTS.md`へ反映した。 | **標準化候補（反映済み）**。置換先を文章化する前に`/skills`とfrontmatterで既存Skillの適用条件を確認し、正本手順との責務境界を決める。 | `開発フロー.md`、preflight/review Skill、`AGENTS.md`、`superpowers利用終了_手順置換_2026-08-12.md`。Skill未導入時も品質ゲートを継続できることを再確認する。 |
| DR-C-029 | Skill配置・Medium | `projectfoundation-preflight-ja`と`projectfoundation-review-ja`がユーザー領域とリポジトリ領域に同名で存在し、SHA-256が一致しないため、ProjectFoundation外や検出順によって古い手順が選ばれる余地がある。 | **保留**。ProjectFoundation内ではリポジトリの`.agents/skills/`と編集元を正本とすることを`AGENTS.md`へ明記した。ユーザー領域は他プロジェクトへ影響し得るため自動同期・削除していない。 | **保留**。同名Skillの配置方針は、ユーザー領域の利用プロジェクトを確認してから統合・削除・改名を判断する。 | `AGENTS.md`、`superpowers利用終了_手順置換_2026-08-12.md`。ユーザー領域のSkill整理を行うとき、またはProjectFoundation外で同名Skillを使う前に再確認する。 |
| DR-C-030 | 文書参照・Low | `開発フロー.md`の節名を「Codex標準機能とSkillの利用」へ変更した後も、preflight/review Skillの参照名4か所が旧名称「Codex標準機能の利用」のまま残っていた。 | **対応済み**。編集元2件と`.agents/skills`配布コピー2件を新名称へ揃えた。 | **既存観点で対応**。文書見出し変更時の参照整合性確認で検出可能。 | preflight/review Skillの正本・配布コピー、`開発フロー.md`、`superpowers利用終了_手順置換_2026-08-12.md`。旧節名検索0件とコピー一致を再確認する。 |

## 2026-08-12 F-011月次集計 preflight独立レビュー

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-F011-BE-001 | 集計null・High | 作業なしの承認済み休日では社員別`SUM(work_minutes)`がnullになり得る。 | **資料対応済み・実装待ち**。`COALESCE`、社員0分、案件/分類空、休日1日の契約とBE-014を追加。 | **個別対応**。F-011の社員集計元とnullable列の組合せに対する対応。 | 正本テスト、実装計画。RT-F011-BE-014成功後に完了判定する。 |
| FIND-F011-BE-002、FIND-F011-BE-003、FIND-F011-BE-004 | 境界・入力・エラー / Medium | 月/年/うるう境界、厳密`yyyy-MM`、400/401/403の具体契約が不足していた。 | **資料対応済み・実装待ち**。具体日付/合計、拡張年等、code/details/requestId/非開示を追加。 | **既存観点で対応**。境界値、入力形式、共通エラーの既存観点で検出可能。 | BE-006/006A/006B/007/008/009/015。各RT成功後に完了判定する。 |
| FIND-F011-BE-005、FIND-F011-BE-006 | ログ・認可 / High | Security前401の月次metadata経路と、403前Repository未呼出しの保証が不足していた。 | **資料対応済み・実装待ち**。path mapping/実ログ捕捉、認可先行/Repository 0回を追加。 | **既存観点で対応**。認可順序と相関ログの既存観点で検出可能。 | BE-011/012/013、実装計画Backend。各RT成功後に完了判定する。 |
| FIND-F011-FE-001、FIND-F011-FE-002 | 日付境界・非同期競合 / High | ローカル月初早朝と遅い旧応答による上書きを検出できなかった。 | **資料対応済み・実装待ち**。FE-009/010へ時刻固定とdeferred応答を追加。 | **既存観点で対応**。タイムゾーン・通信順序の既存観点で検出可能。 | 正本テスト、実装計画Frontend。RT-F011-FE-009/010成功後に完了判定する。 |
| FIND-F011-FE-003、FIND-F011-FE-004、FIND-F011-FE-013 | Frontend責務 / High | 401、ADMIN初期ルート、Page/Appの所有責務が混在・不足していた。 | **資料対応済み・実装待ち**。Page callbackとApp認証破棄を分け、日報UI非併存を追加。 | **既存観点で対応**。テスト責務分割と画面接続の既存観点で検出可能。 | FE-007/012/013、責務対応表。実装後にFocused/App回帰で再確認する。 |
| FIND-F011-FE-005 | アクセシビリティ・High | 4切替のrole/state/keyboard契約が未定義だった。 | **資料対応済み・実装待ち**。WAI-ARIA tabsの自動選択方式とFE-011を追加。 | **既存観点で対応**。既存アクセシビリティ観点を具体UIへ適用。 | FE-011、E2E-001。Unit/Mock E2E成功後に完了判定する。 |
| FIND-F011-FE-006、FIND-F011-FE-007、FIND-F011-FE-008 | E2E・空状態 / High | 非0件E2EのAC-009誤割当、Backend 403との層混在、空表示の抽象性があった。 | **資料対応済み・実装待ち**。正常/非管理者/空をE2E-001～003へ分離。 | **既存観点で対応**。層分離・期待結果の既存観点で検出可能。 | 正本テスト7/8章。3 E2E成功後に完了判定する。 |
| FIND-F011-FE-009 | UI概要・Medium | S-008モックの概要カードをA-016から一意に導出できなかった。 | **資料対応済み・実装待ち**。対象社員数/総作業分だけを契約化し、他カードを対象外とした。 | **個別対応**。F-011のAPIで根拠を持つ表示だけへ限定。 | 正本テスト2章、実装計画2章。利用者承認で確定する。 |
| FIND-F011-FE-010、FIND-F011-FE-011、FIND-F011-FE-012、FIND-F011-FE-014、FIND-F011-FE-015 | 配置・回帰 / High | 日報support混入、ADMIN helper波及、月次mock/CSS配置、早期shared化の判断が不足していた。 | **資料対応済み・実装待ち**。月次専用support/mock/CSS、既存approval回帰、F-011局所部品を計画。 | **既存観点で対応**。FE-015の早期shared化のみ**個別対応**。 | 責務対応表、変更予定ファイル。Full/Mock E2Eで再確認する。 |
| FIND-F011-EXP-001、FIND-F011-EXP-002、FIND-F011-EXP-005、FIND-F011-EXP-009 | 期待結果・High | 正常/状態除外/空/E2Eのfixtureと数値、層割当が具体化されていなかった。 | **資料対応済み・実装待ち**。FIX-F011-001、固有除外値、4空配列、3 E2Eへ具体化。 | **既存観点で対応**。観測可能な期待値と層分離の既存観点で検出可能。 | 正本テスト4/6/7章。該当RT成功後に完了判定する。 |
| FIND-F011-EXP-003、FIND-F011-EXP-004 | 集計意味・High | 社員/案件/分類の集計元と休日「日数」の意味が仮定のままだった。 | **資料対応済み・承認待ち**。社員=日報実勤務、案件/分類=明細、休日=社員日数を承認対象へ明記。 | **個別対応**。F-011受入条件の実装可能な具体化。 | 正本テスト2章、実装計画2章。利用者承認で確定する。 |
| FIND-F011-EXP-006 | トレーサビリティ・High | AC/TC/RTが範囲や「対応RT」で省略され、COMMON/NF/V要件が孤立していた。 | **資料対応済み**。正本8章と統合品質記録をID個別列挙へ変更。 | **既存観点で対応**。完全トレーサビリティの既存観点で検出可能。 | 正本テスト8章、統合品質記録2章。実装後にtest method/spec名を追記する。 |
| FIND-F011-EXP-007 | Oracle E2E・High | 現行runner/verify/cleanupでは承認→月次集計flowを実行できない。 | **資料対応済み・実装/環境待ち**。専用specに加えrunner/verify/cleanupを変更対象へ追加。 | **個別対応**。現行Oracle E2E harness固有。 | ORACLE-001、実装計画4.3。対象commitのE2EOracle成功までF-011完了を保留する。 |
| FIND-F011-EXP-008 | ログ検証・Medium | metadata resolver Unitだけでは実ログの項目・非開示を証明できなかった。 | **資料対応済み・実装待ち**。200/401/403のログ捕捉BE-013を追加。 | **既存観点で対応**。実ログ観測の既存観点で検出可能。 | BE-013。RT成功後に完了判定する。 |

## 2026-08-12 F-011月次集計 実装後レビュー再判定

| 指摘ID | 分類・重要度 | 指摘内容 | 対応状況・対応内容 | 標準化判定 | 反映先・再確認条件 |
| --- | --- | --- | --- | --- | --- |
| FIND-F011-POST-FE-001 | 正常値検出力・P0 | 正常fixtureの案件・分類・休日のIDと数値が部分一致だったため、1行の誤値を検出できなかった。 | **対応済み**。Unit、Mock E2E、Oracle specへ社員・案件・分類・休日の行単位ID→名称→数値の完全一致を追加し、非選択panelのhidden状態も確認した。 | **既存観点で対応**。期待結果の完全一致、UI可視性、E2E検出力の既存観点で横展開可能。 | `frontend/test/MonthlySummaryPage.test.tsx`、`frontend/e2e/monthly-summary.spec.ts`、`frontend/e2e/monthly-summary.oracle.spec.ts`。Focused 23/23、Mock E2E 4/4で再確認済み。 |
| FIND-F011-POST-FE-002 | 空状態契約・P1 | 正本が「固有の空メッセージ」とだけ記載し、実装の共通文言と期待結果が一意でなかった。 | **対応済み**。TC-F011-FE-005を共通文言`該当する集計データはありません。`へ具体化し、Unit/Mock E2Eで可視パネルの文言を確認した。 | **既存観点で対応**。仕様の観測可能化とUI期待結果の既存観点で対応。 | `月次集計_テストケース.md`、`月次集計_テストケースレビュー.md`、月次Page/E2E。再確認条件なし。 |
| FIND-F011-POST-TRACE-001 | トレーサビリティ・P0 | 実装後も品質記録がpreflight未着手状態で、RTと実test名・実行結果が追跡できなかった。 | **対応済み**。統合品質記録、作業記録、実装後レビューを実装済みの状態へ更新し、AC→TC→RT→実test/command→FINDと未実行条件を列挙した。 | **既存観点で対応**。実装後レビュー表の記録・トレーサビリティ観点で検出可能。 | `月次集計_統合品質記録.md`、`月次集計_作業記録.md`、`月次集計_実装後レビュー.md`。Oracle/DBゲートは未達として残す。 |
| FIND-F011-POST-BE-001 | DB集計テスト不足・P0 | BE-002～006、BE-010、BE-012、BE-013の非承認除外、複数行GROUP BY、同日複数社員、空配列、月境界、read-only、認可順序・ログの実DB検証が不足している。 | **未達・保留**。Service/SQL形状と代表Controllerテストは追加済みで、共通Oracle runner経由の`MonthlySummaryControllerTest`は7/7（Failures 0 / Errors 0）で成功した。代表1行を超えるDB結果・境界・read-only・ログの証跡は未作成。 | **標準化候補**。AC→TC→RTを集計軸・状態・境界・副作用ごとに分解し、Repository/Oracle fixtureで完全一致させるマトリクスを標準化する。 | `MonthlySummaryControllerTest.java`、`MonthlySummaryServiceTest.java`、`JdbcMonthlySummaryRepositoryTest.java`。対象Oracle/CIでBE-002～006、010、012、013を実行し、結果とログを回収するまでP0未達。 |
| FIND-F011-POST-GATE-001 | Oracle統合ゲート・P0 | F-011承認→月次Oracle E2E（実画面・Backendログ・verify/cleanup）が未実行で、F-011-AC-012を完了扱いにできない。Controller DB統合テストのwrapper成功はこのE2Eゲートを代替しない。 | **未達・保留**。Controller DBテストは共通`backend/scripts/test-oracle.ps1`（`oracle-test.properties`読込）経由で7/7成功した。E2EOracleは固定データのcleanupを伴うため安全制約で未実行であり、Coverage（Frontend/Backend）も未実行・成果物なしのため、AC-012と機能完了は保留。 | **既存観点で対応**。Oracle/E2E環境ゲート、Coverageゲート、失敗伝播の既存運用で扱う。 | `frontend/e2e/monthly-summary.oracle.spec.ts`、`backend/scripts/test-oracle.ps1`、`backend/scripts/test-e2e-oracle.ps1`、verify/cleanup、`月次集計_統合品質記録.md`。対象commitを資格情報付きOracle CIでE2EOracleとCoverageを実行し、Backend log/Playwright report/verify/cleanup/coverage成果物を回収する。 |
| FIND-F011-POST-SEC-001 | ログ・相関ID検証不足・P1 | 月次200/403の完了ログ、403 requestIdのヘッダ・本文一致、query/year/name/SQL非出力を自動捕捉する証跡が不足している。 | **未達・保留**。401 metadataのUnitと403レスポンスの代表アサーションはあるが、DB統合ログ捕捉はOracle未実行である。 | **既存観点で対応**。セキュリティ規約の相関ID・機密情報非出力・ログ終端観点で横展開可能。 | `RequestIdFilterTest.java`、`MonthlySummaryControllerTest.java`、`月次集計_実装後レビュー.md`。Oracle/CIで200/401/403のlog captureを追加・実行し、URL query・氏名・SQLの不在を確認する。 |
| FIND-F011-POST-DB-001 | Oracle日付上限・P2 | `9999-12`の翌月初が`10000-01-01`となり、Oracle DATE上限との組合せが未確認である。 | **保留**。仕様にない年範囲制限は追加せず、YearMonthの厳密形式だけを実装した。 | **標準化候補**。DB日付型の上限境界をOracle実機ケースとして標準化する。 | `MonthlySummaryService.java`、正本BE-006/006A/006B/015。Oracleで`9999-12`の応答またはエラー契約を確認し、仕様判断後に再判定する。 |
| FIND-F011-POST-CONN-001 | DBテストfixture・P0 | 月次Controllerテストの`daily_reports` INSERTが24列に対して23プレースホルダで、接続後にOracle 17003「列索引が無効です」となった。直接Maven実行では共通Oracle設定も読み込まれず、先に`ORA-01017`として観測された。 | **対応済み**。既存機能と同じ`backend/scripts/test-oracle.ps1`で設定ファイルを読み込む実行入口へ統一し、INSERTを24プレースホルダへ修正。共通runnerで`MonthlySummaryControllerTest` 7/7（Failures 0 / Errors 0）を確認した。 | **既存観点で対応**。Oracle共通runner利用、fixture列・引数一致、DB統合テストの失敗原因切り分けとして既存観点で検出可能。 | `backend/src/test/java/com/example/dailyreport/monthlysummary/MonthlySummaryControllerTest.java`、`backend/scripts/test-oracle.ps1`。追加のBE-002～006/010/012/013とOracle E2Eは別保留条件。 |
