min_version: 2.1.10
no_tty: true

pre-commit:
  jobs:
    - name: quick-check
      run: pwsh -NoProfile -File scripts/check.ps1 -Mode Quick

pre-push:
  jobs:
    - name: pre-push-fast-check
      use_stdin: true
      run: pwsh -NoProfile -File scripts/check.ps1 -Mode PrePush
